# Blueprint Command Reference

> Generated by `scripts/generate-command-registry.ts` from `generated/command-catalog.json`. Do not edit this reference by hand; rerun the generator instead.

## Command Counts

- Runnable direct commands: 55
- Retained non-runnable commands: 1

## Intent Chooser

| Intent | Runnable command |
| --- | --- |
| Start fresh | `/blu-new-project` |
| Understand an existing repo | `/blu-map-codebase` |
| Decide next safe action | `/blu-next` |
| Plan implementation | `/blu-plan-phase <phase>` |
| Execute safely | `/blu-execute-phase <phase>` |
| Prepare isolated plan run | `/blu-run-plan <phase> <planId>` |
| Review/fix code | `/blu-code-review <phase>`, `/blu-code-review-fix <phase>` |
| Ship | `/blu-ship` |

## Runnable Commands

These commands are runnable because the live catalog marks them `implemented`.

### Foundation

| Command | Purpose | Risk | Execution profile |
| --- | --- | --- | --- |
| `/blu-health` | `health` checks Blueprint project health and can run explicit repair-mode normalization through MCP-owned tools. | Medium: repair mode can normalize config and rewrite malformed planning artifacts. | `interactive-read` |
| `/blu-help` | `help` shows safe Blueprint router guidance from project readiness and the implemented command catalog. | Low: read-only router guidance from project status and the live command catalog. | `router` |
| `/blu-map-codebase` | `map-codebase` analyzes a brownfield codebase with mapper-style passes and produces the stable seven-document Blueprint codebase bundle. | Medium: refresh mode can replace existing codebase-mapping artifacts. | `long-running-mutation` |
| `/blu-new-project` | new-project initializes a Blueprint project with deep context gathering and PROJECT.md. | Medium: deep-questioning bootstrap that creates the initial planning tree, seeds normalized repo config, and leaves a traceable first roadmap. | `long-running-mutation` |
| `/blu-progress` | `progress` summarizes Blueprint repo status, blockers, warnings, and next safe implemented guidance from MCP-owned state. | Low: read-only status inspection. | `router` |
| `/blu-set-profile` | `set-profile` changes the current project model_profile through the governance MCP config substrate. | Low: single-setting mutation for project model profile selection. | `interactive-read` |
| `/blu-settings` | `settings` inspects and updates Blueprint repo or default configuration through governance MCP tools. | Low: config-only mutation inside repo config plus optional user defaults. | `interactive-read` |

### Core Lifecycle

| Command | Purpose | Risk | Execution profile |
| --- | --- | --- | --- |
| `/blu-discuss-phase <phase>` | `discuss-phase` gathers durable phase context through adaptive discovery, capability-gated gray-area research sidecars, checkpointed resumability, validation repair, and MCP-owned phase artifact writes. | Medium: can replace or extend phase context artifacts. | `long-running-mutation` |
| `/blu-execute-phase <phase>` | `execute-phase` executes saved phase plans in deterministic target order, records plan-linked execution summaries, and syncs Blueprint state without claiming phase completion. | High: drives real repo mutation during implementation and records execution summaries. | `long-running-mutation` |
| `/blu-next` | `next` returns the next safe direct Blueprint command for the current repo state without widening beyond implemented commands. | Low: read-only next-step routing from project status, effective config, state, artifacts, and the live command catalog. | `router` |
| `/blu-pause-work` | `pause-work` records a canonical handoff from current Blueprint state and artifact inventory. | Low: writes handoff and state artifacts only. | `interactive-read` |
| `/blu-plan-phase <phase>` | `plan-phase` creates or extends execution-ready phase plans through MCP-owned structured phase.plan model validation and plan writes. | Medium: can replace plans and change downstream execution order. | `long-running-mutation` |
| `/blu-research-phase <phase>` | `research-phase` gathers phase-scoped implementation guidance from saved Blueprint artifacts, optional spec evidence, repo evidence, and approved external references, then persists validated research through MCP-owned state paths. | Low: writes research artifacts only. | `long-running-mutation` |
| `/blu-resume-work` | `resume-work` restores working context from the canonical pause handoff and records the next safe action. | Low: restores state from the canonical pause handoff and updates the next safe action. | `interactive-read` |
| `/blu-spec-phase <phase>` | `spec-phase` clarifies what a Blueprint phase should deliver and writes the optional phase-scoped spec artifact before later discovery and planning flows. | Low: optional pre-discussion requirements clarification writes one phase-scoped spec artifact and synced state only. | `long-running-mutation` |
| `/blu-ui-phase <phase>` | `ui-phase` creates or reuses the single phase-scoped UI artifact, writing either a UI design contract or an explicit skip rationale through MCP-owned phase artifact persistence. | Low: writes a UI contract or documented skip rationale only. | `long-running-mutation` |
| `/blu-validate-phase <phase>` | `validate-phase` audits saved execution summaries and persists phase verification evidence through the validation MCP substrate. | Low: writes summary-aware verification evidence and updates follow-up state. | `long-running-mutation` |
| `/blu-verify-work <phase>` | `verify-work` runs summary-backed UAT and persists resumable phase UAT evidence through the validation MCP substrate. | Medium: writes resumable UAT artifacts, can close or reopen roadmap completion, and records follow-up state. | `long-running-mutation` |

### Roadmap And Milestone

| Command | Purpose | Risk | Execution profile |
| --- | --- | --- | --- |
| `/blu-add-phase` | Append a new whole-number phase to an initialized Blueprint roadmap through MCP-owned roadmap and scaffold writes. | Medium: appends the next whole-number phase, scaffolds the matching phase directory, and updates the next-step signal. | `interactive-read` |
| `/blu-audit-milestone` | `audit-milestone` compares original milestone intent against saved phase evidence and writes a durable milestone audit report with grouped gaps and traceability notes. | Low: report generation only. | `interactive-read` |
| `/blu-complete-milestone` | `complete-milestone` performs a report-driven closeout gated by saved milestone audit readiness, writes a durable completion report, and routes to milestone summary. | Medium: writes milestone closeout evidence and advances archival routing. | `interactive-read` |
| `/blu-insert-phase` | `insert-phase` inserts urgent work as a decimal phase between existing phases, scaffolds the matching phase context starter, records roadmap evolution state, and routes back to discovery without renumbering later phases. | Medium: inserts the next decimal phase after an integer phase without renumbering later roadmap entries. | `interactive-read` |
| `/blu-list-phase-assumptions <phase>` | `list-phase-assumptions` surfaces read-only pre-planning assumptions about a phase so users can correct misunderstandings before discovery or planning. | Low: read-only analysis. | `interactive-read` |
| `/blu-milestone-summary` | `milestone-summary` builds a durable consolidated milestone spec from saved roadmap, audit, and completion evidence and routes toward the next milestone-start action. | Low: report generation and routing only. | `interactive-read` |
| `/blu-new-milestone` | `new-milestone` starts a new milestone cycle by deriving carry-forward context from the saved consolidated milestone spec, scaffolding starter docs and the first phase context, and preserving historical phase artifacts. | Medium: rewrites milestone starter docs through carry-forward scaffolding and advances state without deleting historical phase artifacts. | `interactive-read` |
| `/blu-plan-milestone-gaps` | `plan-milestone-gaps` creates grouped roadmap phases to close actionable gaps identified by the saved milestone audit, keeping persistence on roadmap and state MCP tools. | Medium: can add multiple phases in one pass. | `interactive-read` |
| `/blu-remove-phase` | `remove-phase` removes a future roadmap phase, deletes its phase directory, renumbers subsequent roadmap and phase artifacts, and re-anchors state on the safest implemented follow-up. | High: deletes a future phase and renumbers later roadmap references plus phase artifacts. | `interactive-read` |

### Capture And Lightweight Execution

| Command | Purpose | Risk | Execution profile |
| --- | --- | --- | --- |
| `/blu-add-backlog` | `add-backlog` appends explicit parking-lot ideas and can reserve a confirmed 999.x phase stub through MCP-owned capture and scaffold writes. | Low: backlog append plus optional stub scaffold. | `interactive-read` |
| `/blu-add-todo` | `add-todo` appends explicit project-local todo items through the capture index MCP tool. | Low: todo index update only. | `interactive-read` |
| `/blu-check-todos` | `check-todos` inspects pending project-local todos and can mark one active or completed through bounded MCP updates. | Low: todo selection and status update only. | `interactive-read` |
| `/blu-debug` | `debug` investigates a concrete issue, persists a durable debug-latest report, and stops at an explicit follow-up gate before todo capture or fix attempts. | Medium: exploratory shell commands and test runs are likely. | `interactive-read -> long-running-mutation when non-trivial` |
| `/blu-explore` | `explore` briefly classifies an idea into note, todo, backlog, roadmap, or no-write and persists only the explicitly confirmed target through MCP tools. | Medium: ideation-first, but confirmed roadmap promotion can append a new active phase. | `interactive-read` |
| `/blu-fast` | `fast` handles genuinely trivial inline execution without subagents, durable reports, or phase artifacts. | Medium: minimal-planning repo mutation path. | `interactive-read` |
| `/blu-note` | `note` appends explicit project-local notes through the capture index MCP tool while keeping unsupported list, promote, and global-note asks in safe suggestion mode. | Low: note capture only. | `interactive-read` |
| `/blu-quick` | `quick` runs bounded quick delivery with optional depth gates, persists durable quick-run evidence, and routes follow-up through implemented Blueprint commands. | High: can execute repo changes with reduced ceremony. | `long-running-mutation` |
| `/blu-review-backlog` | `review-backlog` previews canonical backlog entries, promotes or archives confirmed items, and records the next safe state through MCP-owned transitions. | Medium: can promote backlog items into active roadmap scope. | `interactive-read` |

### Quality And Shipping

| Command | Purpose | Risk | Execution profile |
| --- | --- | --- | --- |
| `/blu-add-tests <phase>` | `add-tests` generates focused repo tests from saved phase evidence and persists validation plus report artifacts through MCP tools. | High: repo test mutation plus verification/report updates. | `long-running-mutation` |
| `/blu-audit-fix <phase>` | `audit-fix` classifies saved review, security, verification, and UAT evidence, applies bounded remediation when not dry-running, persists a durable audit-fix report, and updates state through MCP tools. | High: bounded remediation plus report/state updates. | `long-running-mutation` |
| `/blu-code-review <phase>` | `code-review` reviews source files changed during a Blueprint phase, resolves deterministic scope from executed plan metadata or explicit file paths, honors review settings, audits saved phase evidence, and persists the result through review MCP tools instead of prompt-only file writes. | Low: review artifact generation only. | `long-running-mutation` |
| `/blu-code-review-fix <phase>` | `code-review-fix` applies bounded fixes from saved code-review findings and persists review-fix evidence plus state through MCP tools. | High: selected findings can trigger bounded repo remediation plus review-fix/state updates. | `long-running-mutation` |
| `/blu-docs-update` | `docs-update` refreshes or verifies selected repo documentation against saved Blueprint and repo evidence, optionally checks current external truth, and persists the durable docs-update report through MCP. | Medium: writes selected repo documentation files and a durable docs-update report. | `long-running-mutation` |
| `/blu-impact` | `impact` performs advisory blast-radius analysis for a resolved change scope, persists an impact report bundle when writing is enabled, and renders the requested output format through the impact MCP substrate. | Low: advisory blast-radius report writes only under .blueprint/impact/. | `long-running-mutation` |
| `/blu-pr-branch` | `pr-branch` prepares a clean review branch by filtering Blueprint bookkeeping scope and persists a durable report. | High: git branch mutation. | `high-risk-maintenance` |
| `/blu-review <phase>` | `review` orchestrates bounded peer review from saved phase plans and evidence, preserves reviewer availability and disagreement honestly, and persists the peer-review artifact through review MCP tools. | Medium: external reviewer orchestration without default repo mutation. | `long-running-mutation` |
| `/blu-secure-phase <phase>` | `secure-phase` verifies declared saved-plan threats against completed execution evidence and persists phase security evidence through review MCP tools. | Low: audit artifact only. | `long-running-mutation` |
| `/blu-ship` | `ship` prepares a confirmation-gated shipping run from saved Blueprint evidence and records actual push or PR outcomes. | High: remote and git mutation path. | `high-risk-maintenance` |
| `/blu-ui-review <phase>` | `ui-review` audits shipped UI work against saved execution and UI-spec evidence, optionally delegates bounded six-pillar analysis, and persists the UI-review artifact through review MCP tools. | Low: review artifact only. | `long-running-mutation` |
| `/blu-undo` | `undo` previews a bounded revert, persists a durable undo report, and runs only confirmed safe revert-style git steps. | High: intentionally destructive history-rewrite-adjacent workflow using safe revert-style steps. | `high-risk-maintenance` |

### Plan Run Harness

| Command | Purpose | Risk | Execution profile |
| --- | --- | --- | --- |
| `/blu-run-plan <phase> <planId>` | `run-plan` previews and prepares one saved phase plan for isolated implementation, then later captures authorized implementation diffs as deterministic PlanRun patch records before summary or PR handoff. | High: prepares an isolated branch and worktree for real repo mutation and records PlanRun state. | `long-running-mutation` |

### Workspace And Maintenance

| Command | Purpose | Risk | Execution profile |
| --- | --- | --- | --- |
| `/blu-cleanup` | `cleanup` archives completed Blueprint phase directories through a protected-scope confirmation flow and persists a durable cleanup report. | High: confirmation-gated phase-directory archival and removal behavior. | `high-risk-maintenance` |
| `/blu-new-workspace` | `new-workspace` creates a confirmed multi-repo workspace and records it in host-global Blueprint workspace state. | High: filesystem and git worktree mutation outside the current repo. | `high-risk-maintenance` |
| `/blu-reapply-patches` | `reapply-patches` previews, confirms, replays, and records host-global Blueprint patch reapplication. | High: confirmation-gated patch replay across repo files. | `high-risk-maintenance` |
| `/blu-remove-workspace` | `remove-workspace` tears down an exact confirmed workspace and updates the host-global workspace registry. | High: confirmation-gated workspace teardown and registry cleanup. | `high-risk-maintenance` |
| `/blu-update` | `update` inspects the installed Blueprint extension and prepares an advisory out-of-band update checklist. | Low: advisory only; no in-session self-update. | `interactive-read` |
| `/blu-workstreams` | `workstreams` lists, creates, switches, resumes, or completes project-local Blueprint workstreams through MCP-owned state. | Medium: project-local state mutation with switching semantics. | `interactive-read` |

## Unavailable Retained Commands

The commands below are retained in the registry, but they are not runnable in the current runtime and must not be recommended as executable `/blu-*` commands.

| Command | Status | Why not runnable |
| --- | --- | --- |
| `/blu-do` | `repairing` | Not runnable: Missing command manifest: commands/blu-do.toml |
