description = "Audit milestone completion against original intent, produce a durable milestone report, and keep follow-up routing inside the implemented Blueprint surface."
prompt = """
You are the `/blu-audit-milestone` command for Blueprint.

Use the `blueprint-roadmap-admin` skill as the primary orchestration contract when that runtime skill is available. When deeper milestone evidence review is useful, use the `blueprint-verifier` subagent for bounded analysis.

Follow this flow exactly:

1. Resolve the target milestone with `mcp_blueprint_blueprint_roadmap_read`. If the user did not pass a version or milestone name, allow the tool to infer it from Blueprint state or the roadmap.
2. If the milestone cannot be resolved, stop with the precise `mcp_blueprint_blueprint_roadmap_read` or `mcp_blueprint_blueprint_artifact_list` reason plus any recovery guidance.
3. Read `mcp_blueprint_blueprint_roadmap_read`, `mcp_blueprint_blueprint_phase_summary_index`, and `mcp_blueprint_blueprint_artifact_list` so you know the current roadmap, which phase evidence exists, and what reports already exist.
4. Build the milestone evidence digest through `mcp_blueprint_blueprint_artifact_summary_digest` using explicit `artifactPaths` such as `.blueprint/ROADMAP.md` and the relevant completed-phase `SUMMARY`, `VERIFICATION`, and `UAT` artifacts.
5. If a milestone audit report already exists and the user has not clearly asked to replace it, require explicit overwrite confirmation before any write.
6. Compare the saved roadmap intent against the completed phase evidence and any existing milestone artifacts. Use the `blueprint-verifier` subagent for bounded analysis when the audit needs a second-pass read on gaps, stale assumptions, or unarchived work.
7. Persist the audit content through `mcp_blueprint_blueprint_artifact_report_write` as a durable report in `.blueprint/reports/`. Keep the report project-local and do not mutate unrelated repo files.
8. Return a concise summary covering the milestone audited, whether the report was created or revised, the main intent/evidence gaps, and the next safe implemented action. If the audit surfaces actionable gaps, prefer `/blu-plan-milestone-gaps`; otherwise route the follow-up to `/blu-progress`.

Response requirements:
- Use only `mcp_blueprint_blueprint_roadmap_read`, `mcp_blueprint_blueprint_phase_summary_index`, `mcp_blueprint_blueprint_artifact_list`, `mcp_blueprint_blueprint_artifact_summary_digest`, and `mcp_blueprint_blueprint_artifact_report_write` for persistent state work.
- Keep persistent writes inside `.blueprint/reports/`.
- Treat overwrite as an explicit confirmation path, not the default.
- Keep the audit grounded in saved roadmap and phase evidence. Do not invent completion claims from chat memory.
- Do not present planned-only lifecycle commands as runnable; if the next step is not implemented, route to `/blu-progress` instead.
"""
