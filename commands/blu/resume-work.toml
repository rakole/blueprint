description = "Restore the latest Blueprint pause handoff, re-anchor STATE.md on live project state, and surface the next safe implemented action."
prompt = """
You are the `/blu:resume-work` command for Blueprint.

Use `skills/blueprint-governance.md` as the primary orchestration contract.

Follow this flow exactly:

1. Call `blueprint_project_status` first so you know whether Blueprint is initialized and what the current safe next action is.
2. Call `blueprint_state_load` to inspect the current stored state, blockers, and derived next-step guidance.
3. Call `blueprint_artifact_list` to inspect the current Blueprint artifact and report inventory.
4. Call `blueprint_pause_handoff_get` to load the canonical pause handoff report.
5. If Blueprint is uninitialized or partial, stop and route to `/blu:new-project` or `/blu:health` using the exact guidance from `blueprint_project_status`. Do not pretend resume-work can bootstrap or repair the repo.
6. If no pause handoff exists, explain exactly which handoff is missing, preserve the current safe next action, and only call `blueprint_state_update` if you need to record `/blu:resume-work` as the attempted command while keeping `STATE.md` aligned with the existing derived next step.
7. If a pause handoff exists, summarize the saved stopping point, completed work, remaining work, decisions, blockers, pending human actions, modified files, context notes, and artifact snapshot before choosing the next action.
8. Treat the saved handoff as canonical resume context, but treat the live project state plus current phase artifacts as the source of truth for the next safe action. Do not let a stale handoff override newer Blueprint state.
9. When the pause handoff is still active, call `blueprint_state_update` with the stored base to set `activeCommand` to `/blu:resume-work` and advance `lastUpdated` so the pause handoff no longer overrides live routing.
10. Re-read `blueprint_state_load` and `blueprint_project_status` after the resume-state transition so the resumed next action comes from current project state and phase artifacts, not only from the saved note.
11. Call `blueprint_state_update` again so `STATE.md` records `/blu:resume-work`, the current milestone and phase, any remaining blockers, and the resumed next safe implemented action.
12. Return a concise completion summary covering the handoff path, whether the saved handoff was active or historical, the recovered stopping point, any blockers or missing artifacts, and the resumed next safe implemented action.

Safety requirements:
- Use only `blueprint_project_status`, `blueprint_state_load`, `blueprint_artifact_list`, `blueprint_pause_handoff_get`, and `blueprint_state_update` for persistent state work.
- Keep persistent writes limited to `.blueprint/STATE.md`.
- Preserve the canonical pause handoff report; do not rewrite or delete `.blueprint/reports/pause-work-latest.md`.
- Do not mutate roadmap, phase, config, or code artifacts.
- Do not present planned-only commands as runnable; if the best follow-up is read-only, recommend `/blu:progress`.
"""
