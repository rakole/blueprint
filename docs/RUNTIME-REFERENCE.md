# Blueprint Runtime Reference

## Purpose And Scope

This document is the canonical Blueprint-only runtime reference for retained commands, bounded skills and agents, MCP tool surfaces, and advisory hook coverage.

It exists so Blueprint contracts can be understood and maintained without consulting any legacy workflow files, parity notes, or external command assets.

## Evidence Tags

- `locked`: directly supported by current Blueprint decisions or command specs
- `docs-aligned`: confirmed by current Blueprint docs and tests
- `needs-behavior-audit`: the Blueprint contract is locked, but deeper behavior verification is still queued

## Current Snapshot

- `/blu`, `help`, and `progress` remain implementation-aware and may only recommend commands whose catalog entry is `implemented`.
- The retained runtime is Blueprint-native: `.blueprint/` owns project state, MCP tools own durable state changes, skills own orchestration, and hooks stay advisory.
- The implemented Blueprint runtime now uses dedicated plan index/read/write MCP tools for persisted phase plans.
- Wave 2 roadmap administration, Wave 3 capture and lightweight execution, and the shipped Wave 4 review plus maintenance surfaces all remain locked to their documented command contracts.
- Future metadata changes must stay aligned across `docs/COMMAND-CATALOG.md`, `docs/SKILLS-AND-AGENTS.md`, this runtime reference, and the per-command specs in `docs/commands/`.

## Shared Runtime Contract

These labels define the shared command-facing contract for Blueprint docs and runtime notes. Each command-facing asset should declare exactly one execution profile. Stage and in-flight status labels are named only where that command already surfaces progress or waiting state, so this shared contract does not imply that every command emits richer runtime behavior today.

- Execution profiles: `router`, `interactive-read`, `long-running-mutation`, `high-risk-maintenance`
- Stage vocabulary: `Resolve`, `Read`, `Decide`, `Execute`, `Persist`, `Validate`, `Route`
- In-flight status fields: resolved scope, active stage, pending gate, execution mode, next safe action

## Command Runtime Matrix

The command tables below map each retained Blueprint command to its current runtime contract. Exact skills, agents, and MCP tools come from the existing Blueprint command specs; hook involvement comes from the locked hook policy and each command's declared write surface.

### Wave 0: Foundation

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `new-project` | `docs/commands/new-project.md` | `blueprint-bootstrap` | `blueprint_project_init`<br>`blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_config_set`<br>`blueprint_state_update`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_validate`<br>`blueprint_artifact_scaffold` | `blueprint-project-researcher`<br>`blueprint-roadmapper` | `read-before-edit`; `.blueprint` write guard | Keep MCP-owned bootstrap writes, but preserve higher-level bootstrap intent through `blueprint-bootstrap`: deep questioning, ask_user-backed one-question-at-a-time clarifications when structure helps, Gemini-native topic and todo coordination for long runs, task-tracker-backed dependency management when bootstrap branches, `get_internal_docs` self-correction for host-tool details, saved-defaults-aware config shaping, canonical bootstrap-contract reads, post-write validation against the runtime contract, durable requirements, roadmap revision loops, and explicit brownfield next-step handling without reviving GSD shell choreography. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `settings` | `docs/commands/settings.md` | `blueprint-governance` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_config_set` | none | `read-before-edit`; `.blueprint` write guard | Blueprint settings own both repo config and optional user-default persistence. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `set-profile` | `docs/commands/set-profile.md` | `blueprint-governance` | `blueprint_config_get`<br>`blueprint_config_set_profile` | none | `read-before-edit`; `.blueprint` write guard | Blueprint-native flow contract for model profile selection; do not fabricate a missing earlier workflow reference. | `locked`; `docs-aligned` |
| `/blu` | `docs/commands/root-router.md` | `blueprint-router` | `blueprint_command_catalog`<br>`blueprint_project_status`<br>`blueprint_config_get` | none | none | Host-native root router; when routing is blocked or incomplete, explain the missing prerequisite or blocked-command reason and keep recommendations inside the implemented command surface only. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `help` | `docs/commands/help.md` | `blueprint-router` | `blueprint_command_catalog`<br>`blueprint_project_status` | none | none | Router profile; report the waiting state from project status, keep the next safe action explicit, and never present planned or blocked commands as runnable. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `progress` | `docs/commands/progress.md` | `blueprint-router` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_command_catalog` | none | none | Router profile; preserve read-only next-step guidance while surfacing active profile, branching mode, blockers, pending gates, and config warnings from normalized config, and keep recommendations inside the implemented runtime surface. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `health` | `docs/commands/health.md` | `blueprint-governance` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_config_set`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_artifact_validate`<br>`blueprint_state_sync` | none | `read-before-edit`; `.blueprint` write guard | Health owns config migration and repair for the normalized Blueprint schema. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `map-codebase` | `docs/commands/map-codebase.md` | `blueprint-map` | `blueprint_project_status`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_scaffold`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_codebase_artifact_write`<br>`blueprint_artifact_validate` | `blueprint-mapper` | `read-before-edit`; `.blueprint` write guard | Retained as the only brownfield-mapping command; treat supplied focus areas as targeted deepening across the same seven-document bundle including `STRUCTURE.md`, read the canonical codebase contract before scaffold or refresh decisions, persist substantive mapping content through the dedicated MCP write path, prefer `ask_user` for reuse/replace confirmation, validate the resulting bundle, and do not revive omitted `scan` or `intel` as new user commands. | `locked`; `docs-aligned`; `needs-behavior-audit` |

### Wave 1: Core lifecycle

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `discuss-phase` | `docs/commands/discuss-phase.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_context`<br>`blueprint_roadmap_read`<br>`blueprint_phase_plan_index`<br>`blueprint_artifact_list`<br>`blueprint_config_get`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_artifact_write`<br>`blueprint_phase_checkpoint_get`<br>`blueprint_phase_checkpoint_put`<br>`blueprint_phase_checkpoint_delete`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | `blueprint-researcher` | `read-before-edit`; `.blueprint` write guard | The shipped Phase 3 slice now does a prior-context sweep across `PROJECT.md`, `REQUIREMENTS.md`, `STATE.md`, earlier phase artifacts, and codebase summaries before it asks questions; it uses one-question `ask_user` branching, stronger assumptions-mode analysis, methodology lenses, progress recaps for session legibility, checkpoint-per-area persistence, canonical plan-index and artifact-contract reads, deferred-idea folding, and end-of-run `STATE.md` updates while later chaining and power-mode variants stay blocked until downstream lifecycle substrate exists. The contract does not promise a dedicated todo/backlog file crawl. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `research-phase` | `docs/commands/research-phase.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_context`<br>`blueprint_phase_research_status`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_artifact_write`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_load`<br>`blueprint_command_catalog`<br>`blueprint_state_update` | `blueprint-researcher` | `read-before-edit`; `.blueprint` write guard | Existing research now follows an explicit ask_user-backed `view`/`skip`/`update` path, validated phase research is persisted through MCP instead of a scaffold-only write, and runtime-aware routing stays limited to implemented commands. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `ui-phase` | `docs/commands/ui-phase.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_research_status`<br>`blueprint_config_get`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_artifact_write`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | `blueprint-ui-designer`<br>`blueprint-checker` | `read-before-edit`; `.blueprint` write guard | Read the canonical `phase.ui-spec` contract before drafting or persisting, keep designer output bounded by checker review, and use `XX-UI-SPEC.md` as the single durable output for either a UI contract or an explicit skip rationale. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `plan-phase` | `docs/commands/plan-phase.md` | `blueprint-phase-planning` | `blueprint_phase_locate`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_context`<br>`blueprint_phase_research_status`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_plan_read`<br>`blueprint_phase_plan_write`<br>`blueprint_config_get`<br>`blueprint_artifact_scaffold`<br>`blueprint_artifact_validate`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-planner`<br>`blueprint-checker` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, require a structured reuse/revise/replace gate before overwrite, and keep real `XX-YY-PLAN.md` persistence on the dedicated MCP path with normalized-config planning behavior. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `execute-phase` | `docs/commands/execute-phase.md` | `blueprint-phase-execution` | `blueprint_phase_locate`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_plan_read`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_summary_write`<br>`blueprint_config_get`<br>`blueprint_artifact_validate`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-executor` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, pair Gemini-native `update_topic` and `write_todos` for long execution runs without turning them into persistence, reads the canonical `phase.summary` contract before any summary write or replacement, and stops later-wave execution when lower-wave gaps remain. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `validate-phase` | `docs/commands/validate-phase.md` | `blueprint-phase-validation` | `blueprint_phase_locate`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_validation_read`<br>`blueprint_phase_validation_write`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_artifact_validate`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-verifier` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, keep overwrite or missing-summary prerequisites explicit as pending gates, read saved execution summaries before verifier analysis or replacement decisions, and persist phase-scoped `XX-VERIFICATION.md` evidence through the validation MCP substrate without drifting from the summary-aware contract. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `verify-work` | `docs/commands/verify-work.md` | `blueprint-phase-validation` | `blueprint_phase_locate`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_validation_read`<br>`blueprint_phase_validation_write`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_artifact_validate`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-verifier` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, keep review/skip/stop checkpoints explicit while conversational UAT is still bounded and in flight, require saved execution summaries plus ready validation evidence before UAT persistence, and keep resumable `XX-UAT.md` checkpoint state plus optional follow-up fixes on the validation MCP substrate instead of mutating unrelated files. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `next` | `docs/commands/next.md` | `blueprint-router` | `blueprint_project_status`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_command_catalog` | none | none | Host-native router flow; report waiting state and the next safe follow-up explicitly, and never hide destructive behavior behind implicit routing. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `pause-work` | `docs/commands/pause-work.md` | `blueprint-governance` | `blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_pause_handoff_get`<br>`blueprint_pause_handoff_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now keeps a single MCP-owned handoff report in `.blueprint/reports/` and intentionally omits the automatic WIP git commit. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `resume-work` | `docs/commands/resume-work.md` | `blueprint-governance` | `blueprint_project_status`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_pause_handoff_get`<br>`blueprint_state_update` | none | none | Shipped Blueprint-native flow contract for re-entry/handoff restoration; reconstructs context from `STATE.md` and the canonical `pause-work` handoff instead of fabricating a missing earlier workflow reference. | `locked`; `docs-aligned` |

### Wave 2: Roadmap and milestone management

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `add-phase` | `docs/commands/add-phase.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_roadmap_add_phase`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now implements the append-only roadmap phase append flow: read the roadmap, derive the next integer from the highest base phase number while ignoring decimal suffixes, scaffold `.blueprint/phases/<phase-slug>/`, and update state. Keep the remaining milestone-management commands gated until their own runtime surfaces land. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `insert-phase` | `docs/commands/insert-phase.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_roadmap_insert_phase`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now implements the retained urgent-insertion flow as a roadmap-driven decimal insert: require an existing integer anchor, derive the next decimal from roadmap state, insert the new phase without renumbering later phases, scaffold the matching phase directory from the returned `phasePrefix`, record the inserted decimal in `STATE.md` through `roadmapEvolutionNotes`, and route to `/blu-discuss-phase <decimal>`. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `remove-phase` | `docs/commands/remove-phase.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_phase_locate`<br>`blueprint_roadmap_remove_phase`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now implements a future-only roadmap removal flow: preview the target phase through phase location, ask for explicit confirmation before deletion, require a second explicit destructive confirmation before any execution-evidence-backed force removal, delete the matching phase directory, renumber later roadmap references plus phase-scoped artifact filenames, and re-anchor state on `/blu-progress`. Keep explicit confirmation and execution-evidence safeguards while auditing deeper behavior parity later. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `list-phase-assumptions` | `docs/commands/list-phase-assumptions.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_context`<br>`blueprint_roadmap_read`<br>`blueprint_project_status` | `blueprint-researcher` | none | Behavior audit still needed before claiming full parity. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `plan-milestone-gaps` | `docs/commands/plan-milestone-gaps.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_roadmap_add_phase`<br>`blueprint_state_update` | `blueprint-roadmapper` | `read-before-edit`; `.blueprint` write guard | Blueprint now implements the gap-closure intent: locate the matching milestone audit, preserve grouped requirement, integration, flow, and optional sections with any traceability repair notes, append coherent phases through repeated roadmap-add-phase calls, and route the user to `/blu-discuss-phase <first new phase>`. The roadmap-add path stays state-only and does not mutate code or git history. | `locked`; `docs-aligned` |
| `audit-milestone` | `docs/commands/audit-milestone.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_phase_summary_index`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write` | `blueprint-verifier` | `read-before-edit`; `.blueprint` write guard | Blueprint now ships a milestone-audit report flow that compares original milestone intent against completed phase evidence before archival. The command now reads `report.milestone-audit` before drafting, keeps grouped gap sections plus traceability notes for downstream repair, and uses `ask_user` for overwrite confirmation. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `complete-milestone` | `docs/commands/complete-milestone.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now ships a report-driven milestone closeout step: require the saved milestone audit, read `report.milestone-complete` before drafting, write `milestone-complete-<version>.md`, and route the repo to `/blu-milestone-summary <milestone>` without inventing a new milestone-state engine. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `milestone-summary` | `docs/commands/milestone-summary.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now ships a report-driven milestone summary step that uses saved audit and completion evidence, reads `report.milestone-summary` before drafting, writes `milestone-summary-<version>.md`, and routes to `/blu-new-milestone` without pulling in later-wave docs agents. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `new-milestone` | `docs/commands/new-milestone.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | `blueprint-roadmapper` | `read-before-edit`; `.blueprint` write guard | Blueprint now ships a carry-forward milestone-start flow that uses the saved milestone summary as durable input, reads `report.milestone-summary` before seeding, reads `phase.context` before scaffolding the first carried-forward phase, preserves historical phase artifacts, starts at the next whole-number phase, and routes to `/blu-discuss-phase <first phase>`. | `locked`; `docs-aligned`; `needs-behavior-audit` |

### Wave 3: Capture and lightweight execution

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `note` | `docs/commands/note.md` | `blueprint-capture` | `blueprint_artifact_mutate_index` | none | `read-before-edit`; `.blueprint` write guard | Keep notes project-local; do not reintroduce global-note behavior. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `add-todo` | `docs/commands/add-todo.md` | `blueprint-capture` | `blueprint_artifact_mutate_index` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now ships the minimal project-local todo capture slice: require an explicit description, append through the capture index MCP tool, preserve duplicates instead of rewriting them, and route missing-project cases to `/blu-new-project` while deeper behavior parity remains under audit. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `check-todos` | `docs/commands/check-todos.md` | `blueprint-capture` | `blueprint_artifact_mutate_index`<br>`blueprint_project_status` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now ships a deterministic todo-review slice: list pending todos through the capture index MCP tool, optionally mark one todo active or completed after explicit confirmation, and keep missing-project handling on `/blu-new-project` while deeper behavior parity remains under audit. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `add-backlog` | `docs/commands/add-backlog.md` | `blueprint-capture` | `blueprint_artifact_mutate_index`<br>`blueprint_artifact_scaffold` | none | `read-before-edit`; `.blueprint` write guard | Blueprint-native flow contract; keep backlog append/promotion logic inside Blueprint artifacts and MCP. | `locked`; `docs-aligned` |
| `review-backlog` | `docs/commands/review-backlog.md` | `blueprint-capture` | `blueprint_artifact_mutate_index`<br>`blueprint_roadmap_promote_backlog`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint-native flow contract for backlog promotion. Keep the backlog promotion logic inside Blueprint artifacts and MCP. | `locked`; `docs-aligned` |
| `fast` | `docs/commands/fast.md` | `blueprint-phase-execution` | `blueprint_project_status`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Interactive-read profile for trivial inline execution: keep the ask genuinely small, explicitly exclude tracker-backed branching plus `update_topic` or `write_todos` long-running visibility, refuse to add report-backed or subagent depth, update `STATE.md` only when Blueprint is initialized, and route anything larger to `quick` or phase-planning. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `quick` | `docs/commands/quick.md` | `blueprint-phase-execution` | `blueprint_project_status`<br>`blueprint_command_catalog`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | `blueprint-researcher`<br>`blueprint-planner`<br>`blueprint-executor`<br>`blueprint-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile for non-trivial bounded quick runs; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, require explicit opt-in for deeper discuss, research, or validation passes, treat branchy quick work as tracker-eligible session-local coordination paired with visible todos, persist a durable `quick-run-latest` report, and do not let quick impersonate saved planning or broad lifecycle execution. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `do` | `docs/commands/do.md` | `blueprint-router` | `blueprint_command_catalog`<br>`blueprint_project_status` | none | none | Host-native router flow; map freeform intent to retained commands only. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `explore` | `docs/commands/explore.md` | `blueprint-capture` | `blueprint_artifact_mutate_index`<br>`blueprint_roadmap_add_phase`<br>`blueprint_project_status` | `blueprint-researcher` | `read-before-edit`; `.blueprint` write guard | Behavior audit still needed before claiming full parity. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `debug` | `docs/commands/debug.md` | `blueprint-debug` | `blueprint_project_status`<br>`blueprint_artifact_report_write`<br>`blueprint_artifact_mutate_index`<br>`blueprint_state_update` | `blueprint-debugger` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Interactive-read profile for evidence-backed investigations; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action explicit when diagnose-only confirmation, report overwrite, todo capture, or implemented-command rerouting is pending, persist a durable `debug-latest` report, require an explicit follow-up gate before todo capture or fix attempts, and route broader recovery into implemented execution commands instead of inventing hidden debug state. | `locked`; `docs-aligned`; `needs-behavior-audit` |

### Wave 4: Quality and shipping

`code-review`, `code-review-fix`, `audit-fix`, `secure-phase`, `review`, `ui-review`, `docs-update`, `add-tests`, `pr-branch`, `ship`, and `undo` are currently shipped in this wave. Cleanup remains the shipped Wave 5 maintenance command.

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `code-review` | `docs/commands/code-review.md` | `blueprint-review` | `blueprint_config_get`<br>`blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_review_scope`<br>`blueprint_review_record` | `blueprint-reviewer` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for deterministic phase-scoped review: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use Gemini-native `update_topic` and `write_todos` for non-trivial review runs without turning them into persistence, honor normalized-config review depth defaults, keep explicit scope or overwrite confirmation when broad scope or existing review evidence needs approval, derive repo files from executed plan metadata or explicit file paths without widening explicit `--files` scope, draft `XX-REVIEW.md` against the canonical review contract, and persist the finished artifact through the shared review MCP substrate without exposing the fixer path early. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `code-review-fix` | `docs/commands/code-review-fix.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_review_load_findings`<br>`blueprint_artifact_contract_read`<br>`blueprint_review_record`<br>`blueprint_state_update` | `blueprint-reviewer` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile for bounded review remediation: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use Gemini-native `update_topic` and `write_todos` for non-trivial review-fix runs without turning them into persistence, confirm overwrite or finding selection explicitly before mutation, let execution mode reflect explicit selection, `--all`, or bounded `--auto` plus inline or reviewer-assisted remediation, load saved `XX-REVIEW.md` findings first, draft `XX-REVIEW-FIX.md` against the canonical review-fix contract, persist it with canonical `Findings Addressed`, `Changes Made`, `Verification`, `Follow-Ups`, and `Next Safe Action` headings through the shared review substrate, keep repo mutation limited to the selected issues, and route follow-up through implemented validation or progress commands. The planned `blueprint-fixer`, per-fix commits, and implicit auto re-review loop are not shipped behavior. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `audit-fix` | `docs/commands/audit-fix.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_review_scope`<br>`blueprint_artifact_report_write`<br>`blueprint_artifact_mutate_index`<br>`blueprint_state_update` | `blueprint-reviewer`<br>`blueprint-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `audit-fix` as a bounded audit-driven remediation loop: classify from saved evidence selected by `--source`, bound mutation via `--severity` plus `--max`, require ask_user confirmation for non-trivial mutation and todo capture, report in-flight remediation progress, stop on first failed fix or required verification, persist an `audit-fix-<phase>` report through `blueprint_artifact_report_write`, and route next steps through implemented validation or progress commands only. The planned `blueprint-fixer` remains unshipped and is not an active required runtime path. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `secure-phase` | `docs/commands/secure-phase.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_plan_read`<br>`blueprint_artifact_contract_read`<br>`blueprint_review_record` | `blueprint-security-auditor` | `read-before-edit`; `.blueprint` write guard | Blueprint now ships a phase-scoped threat-verification flow: require saved execution evidence, read the canonical `review.security` contract before drafting, parse the saved phase threat model from executed plan evidence into a bounded threat register, let the user verify or explicitly accept open threats, block advancement while any threat remains open, and persist `XX-SECURITY.md` through `blueprint_review_record` without opening the rest of the review-family surface early. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `docs-update` | `docs/commands/docs-update.md` | `blueprint-docs` | `blueprint_project_status`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write` | `blueprint-doc-writer`<br>`blueprint-doc-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `docs-update` as a scoped repo-doc refresh or verification flow that stays evidence-backed, keeps Blueprint-owned persistence inside `.blueprint/reports/`, and leaves broad doc sweeps confirmation-gated. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `ui-review` | `docs/commands/ui-review.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_review_record` | `blueprint-ui-auditor` | `read-before-edit`; `.blueprint` write guard | Blueprint now ships a phase-scoped UI audit flow: require saved execution evidence, ground the audit in the saved UI contract when present plus the actual frontend surface, and persist `XX-UI-REVIEW.md` through `blueprint_review_record` without reopening the broader peer-review surface early. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `review` | `docs/commands/review.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_plan_read`<br>`blueprint_review_record` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now ships `review` as a phase-plan peer-review flow: read the saved plan set, keep reviewer availability explicit, preserve reviewer disagreement honestly, and persist `XX-REVIEWS.md` through the shared review substrate without inventing hidden external-tool state. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `add-tests` | `docs/commands/add-tests.md` | `blueprint-phase-validation` | `blueprint_phase_locate`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_validation_read`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_validation_write`<br>`blueprint_artifact_list`<br>`blueprint_artifact_validate`<br>`blueprint_artifact_report_write`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-executor`<br>`blueprint-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `add-tests` as a narrow repo-test generation flow: ground new coverage in saved summaries plus validation or UAT evidence, keep repo mutation scoped to the selected tests, keep the selected scope, pending gates, targeted test result, verification status, and report status explicit while work is in flight, update `XX-VERIFICATION.md` through the validation substrate, and persist an `add-tests-<phase>` report for follow-up. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `pr-branch` | `docs/commands/pr-branch.md` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `pr-branch` as a confirmation-gated clean review-branch preparation flow: inspect repo and config state first, make `.blueprint/` filtering explicit, persist `pr-branch-latest`, and never rewrite the source branch in place. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `ship` | `docs/commands/ship.md` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_phase_locate`<br>`blueprint_config_get`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `ship` as a confirmation-gated shipping flow: inspect project and git state first, ground draft versus ready PR decisions in saved verification plus review evidence, persist `ship-latest`, and leave a durable manual fallback when `gh` cannot create the PR. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `undo` | `docs/commands/undo.md` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `undo` as a confirmation-gated revert flow: resolve the target scope first, persist `undo-latest` before git mutation, make stale evidence impact explicit, and keep git changes limited to safe `git revert` style steps instead of destructive history rewrites. | `locked`; `docs-aligned`; `needs-behavior-audit` |

### Wave 5: Workspace and maintenance

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `new-workspace` | `docs/commands/new-workspace.md` | `blueprint-maintenance` | `blueprint_config_get`<br>`blueprint_workspace_registry_get`<br>`blueprint_workspace_create` | none | `workflow advisory` | Use `~/.<host>/blueprint/workspaces.json`; derive the default workspace root from normalized maintenance config when available. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `remove-workspace` | `docs/commands/remove-workspace.md` | `blueprint-maintenance` | `blueprint_workspace_registry_get`<br>`blueprint_workspace_remove` | none | `workflow advisory` | Use `~/.<host>/blueprint/workspaces.json`; keep deletion bounded and confirmation-gated. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `workstreams` | `docs/commands/workstreams.md` | `blueprint-maintenance` | `blueprint_workstream_list`<br>`blueprint_workstream_mutate`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint-native flow contract; keep switching state project-local under `.blueprint/workstreams/`. | `locked`; `docs-aligned` |
| `cleanup` | `docs/commands/cleanup.md` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `cleanup` as a confirmation-gated phase-directory archival flow: inspect project, roadmap, and saved milestone evidence first, persist `cleanup-latest` before filesystem mutation, protect the current phase plus active roadmap scope, and stop instead of inventing an unsafe archive destination. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `update` | `docs/commands/update.md` | `blueprint-maintenance` | `blueprint_update_check`<br>`blueprint_update_plan` | none | none | Advisor/checklist only; do not self-update the installed extension in-session. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `reapply-patches` | `docs/commands/reapply-patches.md` | `blueprint-maintenance` | `blueprint_patch_list`<br>`blueprint_patch_reapply`<br>`blueprint_patch_record` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint-native flow contract; use `~/.<host>/blueprint/patches/`, not the installed extension copy. | `locked`; `docs-aligned` |

## Hook Surface Matrix

| Hook surface | Blueprint disposition | Why | Runtime location | Evidence state |
|---|---|---|---|---|
| `read-before-edit` advisory | Retain as an advisory Blueprint hook. | Reminds the model to read an existing repo file before editing it and reduces edit-loop mistakes without becoming a hidden executor. | `src/hooks/` bundled to `hooks/`, configured through `hooks/hooks.json` | `locked` |
| `.blueprint` write guard | Retain as an advisory Blueprint hook. | Warns on prompt-injection patterns or malformed `.blueprint/` artifact writes and pairs with MCP validation rather than raw shell writes. | `src/hooks/` bundled to `hooks/`, configured through `hooks/hooks.json` | `locked` |
| `workflow advisory` | Retain as an advisory Blueprint hook. | Warns when repo files are edited directly in a Blueprint-managed session outside the intended command flow; it suggests the right command instead of blocking. | `src/hooks/` bundled to `hooks/`, configured through `hooks/hooks.json` | `locked` |
| statusline hook | Explicitly omit. | Blueprint v1 does not depend on statusline injection or a statusline bridge. | not ported | `locked` |
| context-bridge or statusline-fed warning system | Explicitly omit. | Blueprint v1 keeps warning surfaces explicit and does not rely on hidden context-bridge wiring. | not ported | `locked` |
| extension self-update hooks | Explicitly omit. | `/blu-update` is advisory and Blueprint must not self-mutate the installed extension from inside a running session. | not ported | `locked` |
| installer-managed settings rewrites | Explicitly omit. | Blueprint is installed as a Gemini CLI or Tabnine CLI extension from GitHub, not through a custom installer that rewrites runtime settings. | not ported | `locked` |

## Open Verification Queue


### Highest-value behavior audits

- `validate-phase` and `verify-work`: confirm the exact validation and resumable UAT boundaries now that the summary-aware flow is shipped.
- `quick`: confirm the fast-path boundaries, especially discuss, research, and validation shortcuts.
- `audit-fix`: verify the exact review classification, remediation sequencing, and verification loop.
- `ship`: verify PR creation, review gating, and manual fallback behavior for git and remote operations.
- `undo`: verify the revert and dependency-check contract before implementation deepens.
- `new-workspace` and `remove-workspace`: confirm safety prompts, registry assumptions, and teardown boundaries before implementation.
- `reapply-patches`: verify the patch replay model even though Blueprint already locks the global patch-registry destination.

### Hook behaviors to verify later

- Keep the stdin/stdout fixture contract stable for the three shipped advisory hooks under `src/hooks/` and `hooks/`.
- Confirm there are no hidden hook-like behaviors in retained workflows that should instead become skills, MCP tools, or explicit omissions in Blueprint.
- Re-check high-risk commands that mutate repo files so hook coverage stays aligned with the final implementation boundaries and does not drift into a hidden execution engine.
