# Blueprint Runtime Reference

## Purpose And Scope

This document is the canonical Blueprint-only runtime reference for retained commands, bounded skills and agents, MCP tool surfaces, and advisory hook coverage.

It exists so Blueprint contracts can be understood and maintained without consulting any legacy workflow files, parity notes, or external command assets.

## Evidence Tags

- `locked`: directly supported by current Blueprint decisions or command specs
- `docs-aligned`: confirmed by current Blueprint docs and tests
- `source-owned`: live runtime-contract metadata is owned by source files and bundled runtime inputs rather than standalone docs rows
- `behavior-audited`: final hardening has exercised the live workflow against realistic command/runtime surfaces
- `needs-behavior-audit`: the Blueprint contract is locked, but deeper behavior verification is still queued

## Current Snapshot

- `/blu`, `help`, and `progress` remain implementation-aware and may only recommend commands whose catalog entry is `implemented`.
- Brownfield bootstrap is now map-first: unmapped brownfield and `mapping-incomplete` codebase-only repos route to `/blu-map-codebase`, while valid `mapped-only` repos route to `/blu-new-project` and preserve `.blueprint/codebase/*.md`.
- Checkpoint: Phase 2.2 future-contract drift repair.
- State: closed on 2026-04-11.
- This runtime reference carries forward the closed baseline and repairs discovery parity gaps while active closeout continues.
- The retained runtime is Blueprint-native: `.blueprint/` owns project state, MCP tools own durable state changes, skills own orchestration, and hooks stay advisory.
- The implemented Blueprint runtime now uses dedicated plan index/read/write MCP tools for persisted phase plans.
- Execution now honors normalized parallelization, worktree, and branching config through dedicated plan read and summary persistence tools.
- Wave 2 roadmap administration, Wave 3 capture and lightweight execution, and the shipped Wave 4 review plus maintenance surfaces all remain locked to their documented command contracts.
- `/blu-impact` is an intentional additive Wave 4 advisory/reporting command outside the original 53-command retained baseline. Phase 11 final hardening has exercised its live workflow against implementation-scale command, MCP, docs, skill, artifact-contract, package, test, and `dist` surfaces on top of the registered impact MCP tools, regression coverage, docs, and built `dist` assets for read-only context loading, deterministic surface classification, ownership coverage, dependency coverage, command-substrate checks, planned-command exposure review gates, path-level obligations, dist/build provenance checks, advisory scoring, normalized report validation, bounded report writing, and output rendering.
- Control-plane docs may keep a future command declared `planned` while `blueprint_command_catalog` still derives a non-routable runtime status such as `repairing` or `blocked` from missing runtime pieces.
- Future metadata changes must stay aligned across `docs/COMMAND-CATALOG.md`, `docs/SKILLS-AND-AGENTS.md`, this runtime reference, and the per-command specs in `docs/commands/`.

## Shared Runtime Contract

These labels define the shared command-facing contract for Blueprint docs and runtime notes. Each command-facing asset should declare exactly one execution profile. Stage and in-flight status labels are named only where that command already surfaces progress or waiting state, so this shared contract does not imply that every command emits richer runtime behavior today.

- Execution profiles: `router`, `interactive-read`, `long-running-mutation`, `high-risk-maintenance`
- Stage vocabulary: `Resolve`, `Read`, `Decide`, `Execute`, `Persist`, `Validate`, `Route`
- In-flight status fields: resolved scope, active stage, pending gate, execution mode, next safe action

## Read-Only MCP Resource Contract

Blueprint now exposes the read-only command resource contract and keeps the remaining resource views documented for later rollout. `blueprint://commands/catalog` and the implemented-only `blueprint://commands/<command>/runtime-contract` resource are live today, while the phase, codebase, and reports resource views below remain planned.

Locked resource URIs:

- `blueprint://commands/catalog`
- `blueprint://commands/<command>/runtime-contract`
- `blueprint://phases/<phase>/bundle`
- `blueprint://codebase/bundle`
- `blueprint://reports/latest`

Runtime rules:

- These resources are read-only context surfaces for discovery and grounding. They do not own persistence, confirmation, routing, or write semantics.
- `list_mcp_resources` and `read_mcp_resource` are the preferred read path for the live command resources today, with fallback to the existing direct docs/tool reads when a resource is unavailable.
- Router and progress commands use command manifests, `blueprint-router` input bundles, source-owned runtime metadata, and read-oriented MCP tools. Docs remain control-plane history, not runtime inputs.
- Until they are implemented, discovery-style commands must continue to use existing read-oriented MCP tools and control-plane history directly instead of pretending the resource path exists for the still-planned phase, codebase, and reports views.
- `blueprint://commands/catalog` may mirror the full retained catalog metadata, but `/blu`, `help`, `progress`, and `next` must still recommend only commands whose catalog entry is `implemented`.
- `blueprint://commands/<command>/runtime-contract` is exposed only for commands whose catalog entry is `implemented`, and it must mirror live command-catalog metadata plus the locked command spec and runtime-reference row for that command; it does not become a second command-status authority.
- `blueprint://phases/<phase>/bundle` must mirror saved `.blueprint/` phase-grounding inputs and the resolved phase artifact set, with fallback to `blueprint_phase_context`, direct artifact reads, roadmap/state reads, and local docs until the resource exists.
- `blueprint://codebase/bundle` must mirror the saved seven-document `.blueprint/codebase/` bundle plus artifact-contract truth, with fallback to `blueprint_artifact_list`, `blueprint_artifact_contract_read`, and local files until the resource exists.
- `blueprint://reports/latest` is an index-only surface over saved reports. Report authoring and overwrites remain on `blueprint_artifact_report_write`.
- No writes move onto resource surfaces. Config, roadmap, phase, report, review, and capture persistence remain on the existing Blueprint MCP tool surface.

## Command Runtime Matrix

The command tables below map each retained Blueprint command to its current runtime contract. Exact skills, agents, and MCP tools come from the existing Blueprint command specs; hook involvement comes from the locked hook policy and each command's declared write surface.

### Wave 0: Foundation

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `new-project` | `docs/commands/new-project.md` | `blueprint-bootstrap` | `blueprint_project_init`<br>`blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_config_set`<br>`blueprint_state_update`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_validate` | `blueprint-project-researcher`<br>`blueprint-roadmapper` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation Gemini-native bootstrap. The detailed runtime contract lives in `skills/blueprint-bootstrap/references/bootstrap-runtime-contract.md`, with host-entrypoint, MCP FQN, approval-surface, and Gemini-helper guardrails centralized in `skills/blueprint-bootstrap/references/runtime-guardrails.md`. The live contract stays map-first for brownfield repos: unmapped or `mapping-incomplete` states route to `map-codebase`; valid `mapped-only` states may run `new-project` while preserving `.blueprint/codebase/*.md`. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `settings` | `docs/commands/settings.md` | `blueprint-governance` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_config_set` | none | `read-before-edit`; `.blueprint` write guard | Blueprint settings own both repo config and optional user-default persistence. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `set-profile` | `docs/commands/set-profile.md` | `blueprint-governance` | `blueprint_config_get`<br>`blueprint_config_set_profile` | none | `read-before-edit`; `.blueprint` write guard | Blueprint-native flow contract for model profile selection; do not fabricate a missing earlier workflow reference. | `locked`; `docs-aligned` |
| `/blu` | `commands/blu.toml` | `blueprint-router` | `blueprint_command_catalog`<br>`blueprint_project_status`<br>`blueprint_config_get` | none | none | Host-native root router; when routing is blocked or incomplete, explain the missing prerequisite or blocked-command reason and keep recommendations inside the implemented command surface only. Codebase-only states are intentional: `mapping-incomplete` routes to `/blu-map-codebase`, and `mapped-only` routes to `/blu-new-project`. | `locked`; `source-owned`; `needs-behavior-audit` |
| `help` | `src/mcp/command-runtime-metadata.ts#help` | `blueprint-router` | `blueprint_command_catalog`<br>`blueprint_project_status` | none | none | Router profile; report the waiting state from project status, keep the next safe action explicit, and never present planned or blocked commands as runnable. This includes map-first waiting states: brownfield uninitialized and `mapping-incomplete` route to `/blu-map-codebase`, while `mapped-only` routes to `/blu-new-project`. | `locked`; `source-owned`; `needs-behavior-audit` |
| `progress` | `src/mcp/command-runtime-metadata.ts#progress` | `blueprint-router` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_command_catalog` | none | none | Router profile; preserve read-only next-step guidance while surfacing active profile, branching mode, blockers, pending gates, and config warnings from normalized config, and keep recommendations inside the implemented runtime surface. Brownfield uninitialized and `mapping-incomplete` states point to `/blu-map-codebase`; `mapped-only` points to `/blu-new-project`. | `locked`; `source-owned`; `needs-behavior-audit` |
| `health` | `docs/commands/health.md` | `blueprint-governance` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_config_set`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_artifact_validate`<br>`blueprint_state_sync` | none | `read-before-edit`; `.blueprint` write guard | Health owns config migration and repair for the normalized Blueprint schema. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `map-codebase` | `docs/commands/map-codebase.md` | `blueprint-map` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_scaffold`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_codebase_artifact_write`<br>`blueprint_artifact_validate` | `blueprint-mapper` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for read-heavy brownfield mapping: keep `Resolve`/`Read`/`Decide`/`Execute`/`Persist`/`Validate`/`Route` narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, treat supplied focus areas as targeted deepening across the same seven-document bundle including `STRUCTURE.md`, keep reuse as the default posture while `ask_user` gates any refresh or replace path, read the canonical codebase contract before scaffold or refresh decisions, read `blueprint_config_get` with `scope: "effective"` before any optional mapper decision, use `contract.authoringTemplate` as the heading authority, use `skills/blueprint-map/references/map-runtime-contract.md` as the rich canonical authoring and evidence-density authority, prefer capability-gated `blueprint-mapper` lanes when suitable code-analysis subagents are available, fall back to one-document-at-a-time main-agent authoring when they are not, reject browser/web/search-only agents as substitutes, repair per-artifact invalid write results before moving on, persist substantive mapping content through the dedicated MCP write path, validate the resulting bundle, and do not revive omitted `scan` or `intel` as new user commands. On uninitialized brownfield repos, `map-codebase` may be the first Blueprint write and may create only `.blueprint/codebase/*.md`; successful validation leaves `mapped-only` and routes to `/blu-new-project`. | `locked`; `docs-aligned`; `needs-behavior-audit` |

### Wave 1: Core lifecycle

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `discuss-phase` | `docs/commands/discuss-phase.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_context`<br>`blueprint_roadmap_read`<br>`blueprint_phase_plan_index`<br>`blueprint_artifact_list`<br>`blueprint_config_get`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_artifact_write`<br>`blueprint_phase_checkpoint_get`<br>`blueprint_phase_checkpoint_put`<br>`blueprint_phase_checkpoint_delete`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update`<br>`blueprint_state_load` | `blueprint-researcher` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation phase discovery uses the shared profile in `skills/blueprint-phase-discovery/references/long-running-phase-discovery-profile.md` and the command-specific behavior contract in `skills/blueprint-phase-discovery/references/discuss-phase-runtime-contract.md`. It does a prior-context sweep before asking questions, keeps host-supported structured choices and checkpoint resume-versus-discard gates explicit, supports assumptions-mode analysis, uses capability-gated `blueprint-researcher` sidecars only for one gray area or assumptions pass in lightweight gray-area memo mode, preserves a one-area-at-a-time single-agent fallback with checkpoint-per-area resumability, keeps the `phase.context` model contract as context schema authority, rejects context Markdown fallback, keeps `phase.discussion-log` `contract.authoringTemplate` as discussion-log authority, reads plan-index and artifact-contract guidance before persistence, repairs returned artifact validation issues, folds deferred ideas into the saved record, calls `blueprint_state_update` with synced state followed by `blueprint_state_load`, and does not promise a dedicated todo/backlog file crawl. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `research-phase` | `docs/commands/research-phase.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_context`<br>`blueprint_phase_research_status`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_artifact_scaffold`<br>`blueprint_phase_artifact_write`<br>`blueprint_phase_checkpoint_get`<br>`blueprint_phase_checkpoint_put`<br>`blueprint_phase_checkpoint_delete`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_state_load`<br>`blueprint_command_catalog`<br>`blueprint_state_update` | `blueprint-researcher` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation research loads `skills/blueprint-phase-discovery/references/research-phase-runtime-contract.md` as behavior authority and `phase.research` via `blueprint_artifact_contract_read` / `contract.authoringTemplate` as schema authority. Routing remains implemented-only through refreshed state plus `blueprint_command_catalog` proof. Research checkpoints are owner/mode guarded with `/blu-research-phase` and `research`, and delete stays after write or reuse, synced state update, state load, and implemented-command routing proof. Default execution is parent-only; `blueprint-researcher` is optional only when enabled and sidecar criteria show material help, while the parent owns synthesis, persistence, checkpoints, state, user gates, and routing. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `ui-phase` | `docs/commands/ui-phase.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_research_status`<br>`blueprint_config_get`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_ui_skip_write`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_artifact_write`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_load`<br>`blueprint_command_catalog`<br>`blueprint_state_update` | `blueprint-ui-designer`<br>`blueprint-checker` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for bounded UI-contract drafting: keep `Resolve`/`Read`/`Decide`/`Execute`/`Persist`/`Validate`/`Route` narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, keep contract-versus-skip posture, `workflow.ui_safety_gate` rationale confirmation, overwrite confirmation, checker-requested revision, and MCP validation repair explicit as visible gates, branch early with progressive disclosure so explicit skip mode reads the existing `XX-UI-SPEC.md` and writes through `blueprint_phase_ui_skip_write` without loading the full `phase.ui-spec` contract or scaffold, read the canonical `phase.ui-spec` contract only for real UI-contract drafting, read actual saved context and research bodies when contract-mode status reports them, load `skills/blueprint-phase-discovery/references/ui-phase-runtime-contract.md` as the richness, evidence, fallback, and retry authority, keep `contract.authoringTemplate` as heading/schema authority only for real UI-contract mode, use capability-gated `blueprint-ui-designer` and `blueprint-checker` for design-system evidence plus six-dimension UI quality review, preserve the no-subagent section-by-section fallback, reject browser/web-search/shell-only or generic substitute agents, repair invalid writes or checker-blocked dimensions before completion, and use `XX-UI-SPEC.md` as the single durable output for either a UI contract or an explicit skip rationale. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `plan-phase` | `src/mcp/command-runtime-metadata.ts#plan-phase` | `blueprint-phase-planning` | `blueprint_phase_locate`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_context`<br>`blueprint_phase_research_status`<br>`blueprint_phase_artifact_read`<br>`blueprint_phase_validation_read`<br>`blueprint_review_load_findings`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_plan_read`<br>`blueprint_phase_plan_readiness`<br>`blueprint_phase_plan_authoring_context`<br>`blueprint_phase_plan_validate_model`<br>`blueprint_phase_plan_write`<br>`blueprint_phase_plan_validate`<br>`blueprint_config_get`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-planner`<br>`blueprint-checker` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use `skills/blueprint-phase-planning/references/plan-phase-runtime-contract.md` as the rich behavior contract, prefer `blueprint_phase_plan_readiness` as the compact read-only pre-draft packet with read-set freshness, respect `blueprint_phase_research_status.planningReadiness` as the config-aware pre-draft handoff gate, author `phase.plan` as structured JSON against `blueprint_phase_plan_authoring_context.taskSchema`, write it through `blueprint_phase_plan_write` with `validationMode: "strict"`, `authoringMode: "model-only"`, `returnPlanSetValidation: true`, and server-checked `expectedReadSet` when skipping a duplicate pre-write refresh, use `blueprint_phase_plan_validate_model` only for dry-run previews, repair loops, or checker convergence, refresh authoring context after successful writes, user pauses, subagent returns, or stale read-set freshness, keep real `XX-YY-PLAN.md` persistence on the dedicated MCP path with normalized-config planning behavior, gate reuse/revise/replace only for writes that revise or replace saved plan ids, allow additive new plan ids without an unnecessary overwrite gate, consume saved research instead of live browsing for unstable technical decisions, treat checker review as config-gated rather than mandatory, preserve the one-plan-at-a-time no-subagent fallback, reject browser/web-search-only agents as substitutes, avoid scaffold-placeholder seeding and Markdown fallback from `/blu-plan-phase`, repair model dry-run/write/scoped plan-validation failures through MCP, and never infer completion from `blueprint_phase_plan_write.validation.valid` alone. | `locked`; `runtime-owned`; `needs-behavior-audit` |
| `execute-phase` | `docs/commands/execute-phase.md` | `blueprint-phase-execution` | `blueprint_phase_locate`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_execution_targets`<br>`blueprint_phase_plan_read`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_summary_authoring_context`<br>`blueprint_phase_summary_validate_model`<br>`blueprint_artifact_contract_read`<br>`blueprint_phase_summary_write`<br>`blueprint_config_get`<br>`blueprint_artifact_validate`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-executor` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, pair Gemini-native `update_topic` and `write_todos` for long execution runs without turning them into persistence, fall back to prose for the same structured gate when `ask_user` is unavailable, read the canonical `phase.summary` contract plus the Markdown-first summary authoring context before any summary write or replacement, validate Markdown summary drafts before persistence, use `blueprint_phase_execution_targets` for deterministic target selection plus overwrite and overlap warnings, refuse stale or invalid saved plans, preserve wave order, and keep later-wave work blocked until any lower-wave pending plan is closed, including combined `--gaps-only --wave` runs, use bounded `blueprint-executor` agents only with explicit disjoint write ownership, fall back to one-plan-at-a-time inline execution with MCP-backed summaries and `PARTIAL` or `BLOCKED` summaries as durable carry-forward checkpoint evidence when agents are unavailable or unsafe, run targeted verification plus bounded repair before `COMPLETED` summaries, keep `PARTIAL` and `BLOCKED` summaries pending, rerun the summary index before validation and a synced `blueprint_state_update`, never persist execute-phase reports, and never claim phase completion before validation and verification evidence exists; load `skills/blueprint-phase-execution/references/execute-phase-runtime-contract.md` as the rich execute-phase behavior contract. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `validate-phase` | `src/mcp/command-runtime-metadata.ts#validate-phase` | `blueprint-phase-validation` | `blueprint_phase_locate`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_validation_read`<br>`blueprint_phase_validation_authoring_context`<br>`blueprint_phase_validation_validate_model`<br>`blueprint_phase_validation_write`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_artifact_validate`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-verifier` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, load `skills/blueprint-phase-validation/references/validate-phase-runtime-contract.md`, keep overwrite or missing-summary prerequisites explicit as pending gates, classify State A/B/C, read saved execution summaries before verifier analysis or replacement decisions, build structured requirement/task coverage and gap classification evidence, read the `phase.verification` model contract, validate the structured model against the runtime task schema, persist the same model with `authoringMode: "model-only"` and no Markdown fallback, use MCP `content.text` as compact mirrored structured result JSON when host structured content is hidden, author the `phase.verification` 1.1.0 model with `status` equal to `gateState`, normalize `covered` coverage to `COVERED`, preserve extended validation evidence fields, use capability-gated `blueprint-verifier` with a sequential no-subagent fallback, reject browser/web-search/shell-only or generic substitutes, repair model/write validation failures once through MCP, and persist phase-scoped `XX-VERIFICATION.md` evidence through the validation MCP substrate without drifting from the summary-aware contract. | `locked`; `source-owned`; `needs-behavior-audit` |
| `verify-work` | `src/mcp/command-runtime-metadata.ts#verify-work` | `blueprint-phase-validation` | `blueprint_phase_locate`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_validation_read`<br>`blueprint_phase_validation_authoring_context`<br>`blueprint_phase_validation_validate_model`<br>`blueprint_phase_validation_render`<br>`blueprint_phase_validation_write`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_artifact_validate`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-verifier` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, load `skills/blueprint-phase-validation/references/verify-work-runtime-contract.md`, derive a concrete user-observable UAT test queue from saved summaries plus ready verification evidence, classify plain responses into pass/skipped/blocked/issue with inferred severity, keep blocked prerequisites separate from code gaps, read the `phase.uat` model contract, preserve current test, test matrix, result counts, structured gaps, and in-artifact checkpoint state in `XX-UAT.md`, validate the structured UAT model before writing the same model with `authoringMode: "model-only"`, support a sequential no-subagent fallback, reject browser/web-search/shell-only or generic substitutes, repair model/write validation failures once through MCP, and keep all persistence on the validation MCP substrate instead of mutating unrelated files. | `locked`; `source-owned`; `needs-behavior-audit` |
| `next` | `src/mcp/command-runtime-metadata.ts#next` | `blueprint-router` | `blueprint_project_status`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_command_catalog` | none | none | Host-native router flow; report waiting state and the next safe follow-up explicitly, and never hide destructive behavior behind implicit routing. This includes `/blu-map-codebase` for unmapped brownfield or `mapping-incomplete` and `/blu-new-project` for `mapped-only`. | `locked`; `source-owned`; `needs-behavior-audit` |
| `pause-work` | `docs/commands/pause-work.md` | `blueprint-governance` | `blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_pause_handoff_get`<br>`blueprint_pause_handoff_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Blueprint now keeps a single MCP-owned handoff report in `.blueprint/reports/` and intentionally omits the automatic WIP git commit. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `resume-work` | `docs/commands/resume-work.md` | `blueprint-governance` | `blueprint_project_status`<br>`blueprint_state_load`<br>`blueprint_artifact_list`<br>`blueprint_pause_handoff_get`<br>`blueprint_state_update` | none | none | Shipped Blueprint-native flow contract for re-entry/handoff restoration; reconstructs context from `STATE.md` and the canonical `pause-work` handoff instead of fabricating a missing earlier workflow reference. | `locked`; `docs-aligned` |

### Wave 2: Roadmap and milestone management

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `add-phase` | `docs/commands/add-phase.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_roadmap_add_phase`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded roadmap append: load `skills/blueprint-roadmap-admin/references/add-phase-runtime-contract.md`, keep the command grounded in the live roadmap, preview the next integer phase while ignoring decimal suffixes, choose and confirm durable requirement IDs plus a concrete goal plus 2-5 success criteria, prefer `ask_user` for the exact phase-number and roadmap-metadata confirmation gate, pass the confirmed number as `expectedPhaseNumber`, confirmed IDs as `requirementIds`, objective as `goal`, and criteria as `successCriteria`, keep the waiting state explicit as `phase-number-confirmation` or `stale-phase-number`, persist the append only through the roadmap and scaffold MCP tools, scaffold `${phaseDir}/${phasePrefix}-CONTEXT.md` from returned metadata without treating scaffold text as finished context, preserve the no-subagent fallback, reject browser/web-search/shell-only or generic agents as substitutes, and route the next safe action to `/blu-discuss-phase <phase>` without adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `insert-phase` | `docs/commands/insert-phase.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_roadmap_insert_phase`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded roadmap insertion: use `skills/blueprint-roadmap-admin/references/insert-phase-runtime-contract.md` as the rich behavior contract, require a confirmed integer anchor plus non-empty description plus concrete goal plus 2-5 success criteria plus durable requirement IDs declared in `.blueprint/REQUIREMENTS.md`, reject `none yet` or placeholder requirement mappings, pass the confirmed IDs as `requirementIds`, map the matching requirement rows to the inserted phase, keep decimal numbering roadmap-driven, scaffold only starter `phase.context` content from the returned `phasePrefix`, prefer `ask_user` for the insert confirmation gate, keep the waiting state explicit as `phase-insert-confirmation`, `invalid-insertion-anchor`, or `conflicting-decimal-directory`, preserve the no-subagent fallback and reject browser/web-search/shell-only or generic agents as substitutes, report partial MCP-write failures without hand-editing `.blueprint/`, record the inserted decimal in `STATE.md` through `roadmapEvolutionNotes`, and route to `/blu-discuss-phase <decimal>` without adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `remove-phase` | `docs/commands/remove-phase.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_phase_locate`<br>`blueprint_roadmap_remove_phase`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded roadmap removal: preview the target phase through phase location, prefer `ask_user` for the destructive confirmation gates, keep the waiting state explicit as `future-phase-guard`, `remove-phase-confirmation`, or `force-remove-confirmation`, allow `force: true` only after execution evidence triggers the second explicit approval path, and re-anchor state on `/blu-progress` without adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `list-phase-assumptions` | `docs/commands/list-phase-assumptions.md` | `blueprint-phase-discovery` | `blueprint_phase_locate`<br>`blueprint_phase_context`<br>`blueprint_roadmap_read`<br>`blueprint_project_status`<br>`blueprint_config_get` | `blueprint-researcher` | none | Interactive-read profile for read-only pre-planning synthesis: keep the response grounded in saved phase and roadmap state, preserve the five explicit assumption areas plus uncertainty language, read `blueprint_config_get` with `scope: "effective"` before any optional research sidecar decision, surface missing or blocked phase resolution as a waiting state with valid roadmap phases and the next safe implemented follow-up, and do not widen into writes, hidden planning, or tracker-backed progress behavior. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `plan-milestone-gaps` | `docs/commands/plan-milestone-gaps.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_roadmap_add_phase`<br>`blueprint_state_update` | `blueprint-roadmapper` | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded audit-follow-up planning: locate the matching milestone audit, preserve grouped requirement, integration, flow, and optional sections with traceability repair notes, prefer `ask_user` for the grouped plan confirmation gate, keep the waiting state explicit as `missing-milestone-audit`, `no-actionable-gaps`, or `gap-plan-confirmation`, append coherent phases through repeated roadmap-add-phase calls, and route to `/blu-discuss-phase <first new phase>` without adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `audit-milestone` | `docs/commands/audit-milestone.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_phase_summary_index`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write` | `blueprint-verifier` | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded milestone auditing: compare original milestone intent against completed phase evidence, read `report.milestone-audit` before drafting, keep grouped gap sections plus traceability notes for downstream repair, prefer `ask_user` for overwrite confirmation, keep the waiting state explicit as `milestone-audit-overwrite-confirmation`, and stay report-local in `.blueprint/reports/` without adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `complete-milestone` | `docs/commands/complete-milestone.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded milestone closeout: require the saved milestone audit and `derivedStatus.milestoneAudit.readyForCompletion`, read `report.milestone-complete` before drafting, prefer `ask_user` for overwrite confirmation, keep the waiting state explicit as `missing-milestone-audit`, `milestone-not-ready`, or `milestone-complete-overwrite-confirmation`, write `milestone-complete-<milestone>.md`, and route to `/blu-milestone-summary <milestone>` without adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `milestone-summary` | `docs/commands/milestone-summary.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded milestone summarization: use saved audit and completion evidence, read `report.milestone-summary` before drafting, prefer `ask_user` for overwrite confirmation, keep the waiting state explicit as `missing-milestone-audit`, `missing-milestone-complete`, or `milestone-summary-overwrite-confirmation`, write `milestone-summary-<milestone>.md`, and route to `/blu-new-milestone` without pulling in later-wave docs agents or adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `new-milestone` | `docs/commands/new-milestone.md` | `blueprint-roadmap-admin` | `blueprint_roadmap_read`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_summary_digest`<br>`blueprint_config_get`<br>`blueprint_artifact_scaffold`<br>`blueprint_state_update` | `blueprint-roadmapper` | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for bounded milestone restart: use the saved milestone summary as durable carry-forward input, read `blueprint_config_get` with `scope: "effective"` before any optional roadmapper decision, read `report.milestone-summary` before seeding, preview the exact carry-forward source scope plus first-phase target plus starter-doc overwrite set before mutation, build a typed `Roadmapper Packet` from `digestScope`, `carryForwardFacts`, `requirementTransitionHints`, and a relative `firstPhasePreview` only, keep `parentOwnedResponsibilities` with the parent for digest reads, evidence-scope construction, final milestone naming, final phase numbering and paths, confirmation gates, MCP writes, final response, and routing, forbid MCP writes, hand-editing `.blueprint/`, final `phase.context` authoring, confirmation-gate overrides, and any web, browser, or shell access not granted in the roadmapper frontmatter, pass only that packet to `blueprint-roadmapper` instead of raw reports, chat history, unrestricted files, web search results, browser-only findings, or shell-only substitutes, keep the typed result shape and `roadmapperMode` explicit as `used`, `skipped-disabled`, `skipped-unnecessary`, or `unavailable-fallback`, keep the inline fallback equal in shape and evidence quality, read `phase.context` before scaffolding the first carried-forward phase, prefer `ask_user` for reset-versus-carry-forward and overwrite confirmations, keep the waiting state explicit as `missing-milestone-summary`, `carry-forward-confirmation`, or `starter-doc-overwrite-confirmation`, treat the `blueprint_artifact_scaffold` receipt fields `highestBasePhaseNumber`, `firstPhaseNumber`, `firstPhasePrefix`, `firstPhaseDir`, `firstContextPath`, `deletedPhaseDirectories`, and `renamedPhaseDirectories` as authoritative, preserve historical phase artifacts, and route to `/blu-discuss-phase <first phase>` only after `blueprint_state_update` confirms the matching context path exists, without adopting long-running progress tools. | `locked`; `docs-aligned`; `needs-behavior-audit` |

### Wave 3: Capture and lightweight execution

Capture commands `note`, `add-todo`, `check-todos`, `add-backlog`, `review-backlog`, and `explore` are source-owned/docless for live runtime-contract metadata. Their active runtime contract comes from `src/mcp/command-runtime-metadata.ts`, the matching `commands/blu-*.toml` manifest, and `skills/blueprint-capture/SKILL.md` `input_bundles`; `docs/commands/*.md` pages remain historical/reference material and are not active runtime inputs for these rows.

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `note` | `src/mcp/command-runtime-metadata.ts#note` | `blueprint-capture` | `blueprint_artifact_mutate_index` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for deterministic project-local note capture: require explicit note text, append through the capture index MCP tool, treat returned ids as authoritative, keep notes project-local, and do not adopt `update_topic`, `write_todos`, tracker-backed branching, or other long-running progress posture. Unsupported list, promote, or global-note asks must stop with safe implemented-only guidance. | `locked`; `source-owned`; `needs-behavior-audit` |
| `add-todo` | `src/mcp/command-runtime-metadata.ts#add-todo` | `blueprint-capture` | `blueprint_artifact_mutate_index` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for short project-local todo capture: require an explicit description, append through the capture index MCP tool, report duplicate MCP results instead of creating a second copy, route missing-project cases to `/blu-new-project`, and do not add a long-running progress layer. | `locked`; `source-owned`; `needs-behavior-audit` |
| `check-todos` | `src/mcp/command-runtime-metadata.ts#check-todos` | `blueprint-capture` | `blueprint_artifact_mutate_index`<br>`blueprint_project_status` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for deterministic todo inspection plus bounded status change: list pending todos through the capture index MCP tool, require an explicit confirmation gate before marking one `active` or `completed` unless intent is already unmistakable, keep project-health routing honest, and do not add `update_topic`, `write_todos`, tracker-backed branching, or other long-running progress posture. | `locked`; `source-owned`; `needs-behavior-audit` |
| `add-backlog` | `src/mcp/command-runtime-metadata.ts#add-backlog` | `blueprint-capture` | `blueprint_artifact_mutate_index`<br>`blueprint_artifact_scaffold` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for parking-lot capture: require an explicit description, append through the capture index MCP tool, reserve a `999.x` stub only after an explicit confirmation gate, scaffold only the returned stub path, and keep the flow local and short instead of layering in long-running progress tools. | `locked`; `source-owned` |
| `review-backlog` | `src/mcp/command-runtime-metadata.ts#review-backlog` | `blueprint-capture` | `blueprint_artifact_mutate_index`<br>`blueprint_roadmap_promote_backlog`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for deterministic backlog review: preview canonical backlog entries first, use explicit promote or remove confirmations while keeping `keep` as the default safe path, persist promotions and archival through MCP-backed status transitions, route the next safe implemented action through state update, and keep the review flow free of unnecessary long-running progress posture. | `locked`; `source-owned` |
| `fast` | `docs/commands/fast.md` | `blueprint-phase-execution` | `blueprint_project_status`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Interactive-read profile for trivial inline execution: keep the ask genuinely small, explicitly exclude tracker-backed branching plus `update_topic` or `write_todos` long-running visibility, refuse to add report-backed or subagent depth, update `STATE.md` only when Blueprint is initialized, and route anything larger to `quick` or phase-planning. The rich command-local contract lives in `skills/blueprint-phase-execution/references/fast-runtime-contract.md`. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `quick` | `docs/commands/quick.md` | `blueprint-phase-execution` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_command_catalog`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | `blueprint-researcher`<br>`blueprint-planner`<br>`blueprint-executor`<br>`blueprint-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile for non-trivial bounded quick runs; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, require explicit opt-in for deeper discuss, research, or validation passes, read `blueprint_config_get` with `scope: "effective"` before any optional subagent decision, treat branchy quick work as tracker-eligible session-local coordination paired with visible todos, persist a durable `quick-run-latest` report as model-authored `report.quick-run` schema data with MCP-rendered Markdown and no Markdown fallback, and do not let quick impersonate saved planning or broad lifecycle execution. The rich command-local contract lives in `skills/blueprint-phase-execution/references/quick-runtime-contract.md`. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `do` | `docs/commands/do.md` | `blueprint-router` | `blueprint_command_catalog`<br>`blueprint_project_status` | none | none | Planned direct freeform router contract only: control-plane docs keep `/blu-do` declared `planned`, while the live catalog may still derive `repairing` until the dedicated manifest ships. Keep it non-routable until the catalog marks it `implemented`, use the documented intent taxonomy for repo/status guidance, lightweight capture, ideation, small execution, bounded execution, and planning escalation, recommend the safest implemented follow-up when intent is ambiguous or risky, and never hide maintenance behavior or route to planned, blocked, or repairing commands. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `explore` | `src/mcp/command-runtime-metadata.ts#explore` | `blueprint-capture` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_artifact_mutate_index`<br>`blueprint_roadmap_add_phase`<br>`blueprint_artifact_scaffold` | `blueprint-researcher` | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for short ideation routing: classify the idea into exactly one of note, todo, backlog, roadmap, or no-write, require explicit routing confirmation before any persistence, pass confirmed durable `requirementIds`, concrete `goal`, and 2-5 item `successCriteria` when routing directly to roadmap work, read `blueprint_config_get` with `scope: "effective"` before any optional researcher decision, keep optional researcher use bounded to short context checks, and avoid `update_topic`, `write_todos`, tracker-backed branching, or other long-running progress posture while keeping capture deterministic and local. | `locked`; `source-owned`; `needs-behavior-audit` |

### Wave 4: Quality and shipping

`impact`, `code-review`, `code-review-fix`, `audit-fix`, `secure-phase`, `review`, `ui-review`, `docs-update`, `add-tests`, `pr-branch`, `ship`, and `undo` are currently shipped in this wave. Cleanup remains the shipped Wave 5 maintenance command.
Wave 2 roadmap administration, Wave 3 capture plus lightweight execution, Wave 4 docs and review, and the shipped Wave 5 maintenance surfaces including `new-workspace`, `cleanup`, and `update` all remain locked to their documented command contracts.

`code-review` and `code-review-fix` are source-owned for live runtime-contract
metadata in this wave. Their active runtime contract comes from
`src/mcp/command-runtime-metadata.ts`, the matching `commands/blu-*.toml`
manifest, and `skills/blueprint-review/SKILL.md` plus the local runtime
references. `docs/commands/code-review*.md` remain control-plane history and
reference material, not active runtime inputs for these two rows.

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `impact` | `docs/commands/impact.md` | `blueprint-impact` | `blueprint_impact_config_get`<br>`blueprint_impact_scope_resolve`<br>`blueprint_impact_context_load`<br>`blueprint_impact_analyze`<br>`blueprint_impact_report_write`<br>`blueprint_impact_output_render` | none | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for advisory blast-radius analysis: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, load `skills/blueprint-impact/references/impact-runtime-contract.md`, resolve config before scope-sensitive analysis, resolve staged/working-tree/range/base-head/files/diff-file/CI/seed/description scope through the impact scope tool, load Blueprint context read-only, run deterministic analysis through the impact analyze tool, keep missing ownership/dependency/compliance/test/scope metadata as warnings or unknowns rather than safety, separate impact status from risk and confidence, validate report payloads against the `report.impact` JSON Schema plus runtime narrowing with MCP-owned expected scope fingerprint/source/description, expected files, evidence ids and paths, finding ids, and blocking/warning projection ids before writing bounded `.blueprint/impact/<impact-id>/` bundles, reject nested changed-file-bearing content outside expected files, canonicalize report ordering for idempotent reuse, require explicit overwrite confirmation for changed existing bundles, render human/JSON/Markdown/PR-comment/summary output through the renderer, and never mutate source files, roadmap state, phase state, command-catalog state, PR state, deployment state, or the installed extension directory. Phase 11 self-impact hardening verifies the report catches command, MCP, docs, skill, artifact-contract, package, tests, and `dist` blast-radius signals while keeping missing metadata actionable. | `locked`; `docs-aligned`; `behavior-audited` |
| `code-review` | `src/mcp/command-runtime-metadata.ts#code-review` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_config_get`<br>`blueprint_artifact_contract_read`<br>`blueprint_review_scope`<br>`blueprint_review_load_findings`<br>`blueprint_review_validate_model`<br>`blueprint_review_record` | `blueprint-reviewer` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for deterministic phase-scoped review: keep Resolve/Read/Decide/Execute/Validate/Persist/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use Gemini-native `update_topic` and `write_todos` for non-trivial review runs without turning them into persistence, let `blueprint_review_scope` own review enablement, normalized depth defaults, saved evidence inventory, deterministic repo-file scoping, authoring context, and narrowed task schema, load `skills/blueprint-review/references/code-review-runtime-contract.md` for model-only JSON authoring, depth semantics, evidence richness, capability-gated reviewer use, no-subagent fallback, and MCP retry/repair behavior, repair invalid models against `modelContract.jsonSchema`, the narrowed task schema, and returned diagnostics instead of rendered Markdown shape, keep explicit scope or overwrite confirmation when broad scope or existing review evidence needs approval, fail any invalid explicit `--files` scope instead of silently narrowing it, load saved `XX-REVIEW.md` findings before overwrite decisions, read `blueprint_config_get` with `scope: "effective"` before any optional reviewer decision, validate the authored model through `blueprint_review_validate_model`, and persist the model through `blueprint_review_record` so MCP renders canonical `XX-REVIEW.md` without Markdown fallback. When security still routes first, keep `/blu-code-review-fix <phase>` visible as the secondary queued follow-up when concrete follow-up fixes remain. | `locked`; `source-owned`; `needs-behavior-audit` |
| `code-review-fix` | `src/mcp/command-runtime-metadata.ts#code-review-fix` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_config_get`<br>`blueprint_review_load_findings`<br>`blueprint_review_authoring_context`<br>`blueprint_review_validate_model`<br>`blueprint_review_record`<br>`blueprint_state_update` | `blueprint-reviewer` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile for bounded review remediation: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use Gemini-native `update_topic` and `write_todos` for non-trivial review-fix runs without turning them into persistence, require an explicit finding-selection gate unless the user already gave a narrow fix scope, default unscoped remediation to saved `follow-up` findings, keep repo mutation bounded to the selected findings and implicated files, let execution mode reflect explicit selection, `--all`, or bounded `--auto` plus inline or reviewer-assisted remediation, load saved `XX-REVIEW.md` findings first, load `skills/blueprint-review/references/code-review-fix-runtime-contract.md` for the detailed stage mapping, capability-gated read-only subagent path, one-target-at-a-time no-subagent fallback, retry/repair behavior, output quality criteria, and completion criteria, read `blueprint_config_get` with `scope: "effective"` before any optional reviewer decision, call `blueprint_review_authoring_context` so saved code-review findings plus phase execution plan, summary, dependency evidence, and scoped `targetIds` narrow the `review.review-fix` task schema, author only the schema's camelCase JSON fields, forbid rendered-heading or locked-marker JSON keys, validate through `blueprint_review_validate_model`, persist the same model through `blueprint_review_record`, update `STATE.md` through `blueprint_state_update` with explicit synced patch fields for `activeCommand`, `currentPhase`, and `nextAction`, and route follow-up through implemented validation or progress commands only. No auto-fixer behavior, implicit commits or branches, or hidden iterative re-review loops are shipped. Browser/web/search-only substitute agents are also forbidden. | `locked`; `source-owned`; `needs-behavior-audit` |
| `audit-fix` | `docs/commands/audit-fix.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_review_scope`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_artifact_report_authoring_context`<br>`blueprint_artifact_report_validate_model`<br>`blueprint_artifact_report_write`<br>`blueprint_artifact_mutate_index`<br>`blueprint_state_update` | `blueprint-reviewer`<br>`blueprint-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile for bounded audit-driven remediation: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use `update_topic` and `write_todos` for non-trivial audit-fix runs without turning them into persistence, keep ask_user confirmation for non-trivial mutation and todo capture explicit, treat branchy remediation as tracker-eligible session-local coordination paired with visible todos, keep report overwrite or todo capture explicit as waiting gates, load `skills/blueprint-review/references/audit-fix-runtime-contract.md`, keep verification progress, report status, and early-stop state explicit while fixes are in flight, classify from saved evidence selected by `--source` into `auto-fixable`, `manual-only`, and `skip` rows before mutation, read `report.audit-fix` through `blueprint_artifact_contract_read`, read `blueprint_config_get` with `scope: "effective"` before any optional classifier or verifier decision, pass `auditFixContext {source, severity, maxAttempts, dryRun, scopeFiles}` through the report tool flow, author only the model fields `status`, `readiness`, `completionState`, `remediationSummary`, `summaryEvidence`, `classification`, `changesApplied`, `verification`, `pendingPlans`, `dependencyPlans`, `manualOrDeferredWork`, `gapRoutes`, `followUpFixes`, `evidence`, `commitTraceability`, `todoCapture`, and `nextSafeAction`, validate through `blueprint_artifact_report_validate_model`, repair invalid models by exact diagnostic `path`, `code`, `repair`, `allowedValues`, `missing`, `argsPatch`, and `repairSummary` guidance, reread authoring context when runtime context is stale or incomplete, persist the same validated model through `blueprint_artifact_report_write`, preserve the locked wording `Status`, `Readiness`, `Completion State`, `Source`, `Severity Filter`, `Max Attempts`, `Dry Run`, `Evidence Used`, `Fix Scope`, `Changes Applied`, `Remaining Gaps`, and `Next Safe Action`, use `blueprint-reviewer` only for read-only classification and `blueprint-verifier` only for bounded post-fix verification, preserve the single-agent no-subagent fallback that processes one finding at a time with carry-forward compression, reject browser/web-search/shell-only or generic agents as substitutes, and stop after one failed retry with top diagnostics plus `suggestedRepairs`. The planned `blueprint-fixer` remains unshipped and is not an active required runtime path. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `secure-phase` | `docs/commands/secure-phase.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_plan_read`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_execution_targets`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_review_authoring_context`<br>`blueprint_review_validate_model`<br>`blueprint_review_record` | `blueprint-security-auditor` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for phase-scoped threat verification: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use `update_topic` and `write_todos` for non-trivial secure-phase runs, load `skills/blueprint-review/references/secure-phase-runtime-contract.md`, keep the saved-plan threat model only as the bounded evidence source, incorporate execution-summary threat flags without inventing new plan threats, read `blueprint_config_get` with `scope: "effective"` before any optional auditor decision, require completed execution summaries, block pending plans before persistence, author `review.security` as model-only JSON against the narrowed live plan/summary/threat/evidence task schema with lowercase threat statuses such as `closed`, `accepted`, `open`, and `none` plus object-shaped `auditTrail`, validate the model through `blueprint_review_validate_model` before `blueprint_review_record`, reject Markdown fallback, keep overwrite confirmation, verify-versus-accept decisions, or a visible `pending-open-threat` waiting state explicit, make inline versus security-auditor-assisted verification visible, preserve the sequential no-subagent fallback when the auditor is unavailable, keep pending-open-threat status explicit, distinguish confirmed mitigations, open threats, accepted risks, suspicious artifact content, and hardening follow-ups, block next-step routing while threats remain open, and route only to implemented follow-up commands once the threat gate is cleared. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `review` | `docs/commands/review.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_phase_plan_index`<br>`blueprint_phase_plan_read`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_execution_targets`<br>`blueprint_review_authoring_context`<br>`blueprint_review_validate_model`<br>`blueprint_review_record` | `blueprint-reviewer` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for phase-plan peer review: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use `update_topic` and `write_todos` for non-trivial review runs, load `skills/blueprint-review/references/review-runtime-contract.md`, use `review.peer-review` from `blueprint_artifact_contract_read` plus `blueprint_review_authoring_context` as the model/schema authority, build a saved-evidence reviewer packet from selected plans plus available roadmap, requirements, context, research, and related artifact evidence, read `blueprint_config_get` with `scope: "effective"` before any optional reviewer decision, keep reviewer availability, partial reviewer coverage, disagreement posture, and artifact reuse or revision status explicit, keep overwrite confirmation, reviewer-availability confirmation, or the visible `reviewer-availability` waiting state explicit, make explicit reviewer flags versus `--all` fan-out visible, validate the structured peer-review model before persistence, use `blueprint-reviewer` only for read-only packet completeness or consensus/disagreement synthesis, preserve the sequential no-subagent fallback, reject browser/web-search/shell-only or generic substitute agents, repair invalid peer-review models once against the narrowed task schema, preserve disagreement faithfully, and persist MCP-rendered `XX-REVIEWS.md` through the shared review substrate. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `ui-review` | `docs/commands/ui-review.md` | `blueprint-review` | `blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_review_authoring_context`<br>`blueprint_review_validate_model`<br>`blueprint_review_record` | `blueprint-ui-auditor` | `read-before-edit`; `.blueprint` write guard | Long-running-mutation profile for phase-scoped UI audit: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use `update_topic` and `write_todos` for non-trivial ui-review runs, load `skills/blueprint-review/references/ui-review-runtime-contract.md`, use `review.ui-review` from `blueprint_artifact_contract_read` plus `blueprint_review_authoring_context` as the model schema authority, read `blueprint_config_get` with `scope: "effective"` before any optional auditor decision, keep saved `XX-UI-SPEC.md` coverage and the actual frontend surface explicit, author model-only scored six-pillar evidence with overall `/24`, top priority fixes or explicit pass evidence, visual-evidence limitations when screenshots or runtime inspection are unavailable, validate through `blueprint_review_validate_model`, make inline versus capability-gated `blueprint-ui-auditor`-assisted analysis visible, reject browser/web/search-only substitute agents, preserve the one-pillar-at-a-time no-subagent fallback, repair invalid models once against the narrowed task schema, reject Markdown fallback, and keep artifact create/reuse/revise status plus findings-or-pass posture explicit before persisting MCP-rendered `XX-UI-REVIEW.md` through the shared review substrate. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `docs-update` | `docs/commands/docs-update.md` | `blueprint-docs` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write` | `blueprint-doc-writer`<br>`blueprint-doc-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Long-running-mutation profile for scoped repo-doc refresh or verification: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use `update_topic` and `write_todos` for non-trivial docs-update runs, read `blueprint_config_get` with `scope: "effective"` before any optional doc-writer or doc-verifier decision, keep selected repo docs, source files, tests, and digest-backed Blueprint artifacts explicit as repo truth, keep cited external truth separate and optional, route evidence-light broad refreshes to `/blu-map-codebase`, and persist `docs-update-latest` only through the shared report path. | `locked`; `docs-aligned`; `needs-behavior-audit` |
| `add-tests` | `src/mcp/command-runtime-metadata.ts#add-tests` | `blueprint-phase-validation` | `blueprint_phase_locate`<br>`blueprint_phase_summary_index`<br>`blueprint_phase_summary_read`<br>`blueprint_phase_validation_read`<br>`blueprint_phase_validation_authoring_context`<br>`blueprint_phase_validation_render`<br>`blueprint_artifact_contract_read`<br>`blueprint_config_get`<br>`blueprint_phase_validation_write`<br>`blueprint_artifact_list`<br>`blueprint_artifact_validate`<br>`blueprint_artifact_report_authoring_context`<br>`blueprint_artifact_report_validate_model`<br>`blueprint_artifact_report_write`<br>`blueprint_state_load`<br>`blueprint_state_update` | `blueprint-executor`<br>`blueprint-verifier` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Blueprint now ships `add-tests` as a narrow repo-test generation flow: ground new coverage in saved summaries plus validation or UAT evidence, load `skills/blueprint-phase-validation/references/add-tests-runtime-contract.md`, classify candidate files after reading them, confirm non-trivial classifications and test plans with `ask_user`, read `blueprint_config_get` with `scope: "effective"` before any optional executor or verifier decision, keep repo mutation scoped to selected tests, use capability-gated `blueprint-executor` and `blueprint-verifier` with a one-candidate no-subagent fallback, reject browser/web-search/shell-only substitutes, distinguish passing tests from implementation bugs, test-authoring errors, and blocked checks, render updated `XX-VERIFICATION.md` evidence through the validation substrate, author `add-tests-<phase>` as structured `report.add-tests` JSON against the runtime-narrowed report task schema, reject Markdown report fallback, repair invalid writes once, and keep selected scope, pending gates, targeted result, verification status, and report status explicit while work is in flight. | `locked`; `source-owned`; `needs-behavior-audit` |
| `pr-branch` | `src/mcp/command-runtime-metadata.ts#pr-branch` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_report_write` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | High-risk-maintenance profile for clean review-branch preparation: keep the resolved source branch, source `HEAD`, base branch, candidate review branch, filtered `.blueprint/` scope, active stage, pending gate, execution mode, and next safe action explicit while the flow is waiting on `clean-working-tree`, `review-branch-confirmation`, or report overwrite approval, load `skills/blueprint-maintenance/references/pr-branch-runtime-contract.md`, classify commits into `code-only`, `blueprint-only`, `mixed`, and `empty-after-filter`, preview exact replay mutations plus included and excluded path sets, use `report.pr-branch` as the canonical report authoring template, validate clean branch state plus retained file/commit counts after creation, keep the report-backed pre-mutation posture reviewable alongside `pr-branch-latest`, preserve the no-subagent fallback while forbidding browser/web-search/shell-only substitutes, and never rewrite the source branch in place. | `locked`; `source-owned`; `needs-behavior-audit` |
| `ship` | `src/mcp/command-runtime-metadata.ts#ship` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_phase_locate`<br>`blueprint_config_get`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | High-risk-maintenance profile for branchy shipping flows: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use Gemini-native `update_topic` and `write_todos` for non-trivial shipping runs without turning them into persistence, treat branchy shipping work as tracker-eligible session-local coordination paired with visible todos, read `blueprint_artifact_contract_read` for the canonical `report.ship` contract and use `contract.authoringTemplate` before they persist `ship-latest`, keep local prep plus optional push plus optional PR creation explicit, overwrite `ship-latest` after push or PR attempts so the durable report records actual outcomes and fallback notes, and leave a durable manual fallback when `gh` cannot create the PR. | `locked`; `source-owned`; `needs-behavior-audit` |
| `undo` | `src/mcp/command-runtime-metadata.ts#undo` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_phase_locate`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_contract_read`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | High-risk-maintenance profile for confirmation-gated revert flow: keep `Resolve`/`Read`/`Decide`/`Execute`/`Persist`/`Validate`/`Route` narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, keep preflight blockers explicit as `dirty-working-tree`, `detached-head`, `merge-in-progress`, or `missing-revert-target`, keep `undo-confirmation` and `report-overwrite-confirmation` visible while the flow is waiting, read `blueprint_artifact_contract_read` for the canonical `report.undo` contract and use `contract.authoringTemplate` before they persist `undo-latest` before git mutation begins, overwrite `undo-latest` after the revert attempt so the durable report records actual outcomes and blockers, make stale evidence impact explicit, and keep git changes limited to safe `git revert` style steps instead of destructive history rewrites. | `locked`; `source-owned`; `needs-behavior-audit` |

### Wave 5: Workspace and maintenance

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `new-workspace` | `src/mcp/command-runtime-metadata.ts#new-workspace` | `blueprint-maintenance` | `blueprint_config_get`<br>`blueprint_workspace_registry_get`<br>`blueprint_workspace_create` | none | `workflow advisory` | High-risk-maintenance profile for confirmation-gated workspace creation: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, derive the default workspace root from normalized maintenance config when available, preview the resolved workspace name, path, repo members, strategy, branch, manifest path, and registry mutation plan before mutation, keep host-global workspace state under `~/.<host>/blueprint/workspaces.json`, and keep registry plus disk mutation transactional with an explicit clone fallback approval when `worktree` is unsafe. | `locked`; `source-owned`; `needs-behavior-audit` |
| `remove-workspace` | `src/mcp/command-runtime-metadata.ts#remove-workspace` | `blueprint-maintenance` | `blueprint_workspace_registry_get`<br>`blueprint_workspace_remove` | none | `workflow advisory` | High-risk-maintenance profile for confirmation-gated workspace teardown: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, resolve the exact workspace target from `~/.<host>/blueprint/workspaces.json`, keep `workspace-not-found`, `workspace-path-ambiguity`, `dirty-working-tree`, `registry-drift`, `malformed-workspace-registry`, `ask-user-unavailable`, and `remove-workspace-confirmation` explicit while the flow is waiting, and remove only the confirmed workspace manifest, workspace root, and matching host-global registry entry after clean repo-member teardown. | `locked`; `source-owned`; `needs-behavior-audit` |
| `workstreams` | `src/mcp/command-runtime-metadata.ts#workstreams` | `blueprint-maintenance` | `blueprint_workstream_list`<br>`blueprint_workstream_mutate`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard | Interactive-read profile for project-local workstream switching: keep Resolve/Read/Decide/Persist/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use Gemini-native `ask_user` for interactive selection or confirmation, hard-stop on `dirty-working-tree` when the active stream would change, keep `workstream-switch-confirmation`, `workstream-archive-confirmation`, `missing-workstream`, `missing-resume-snapshot`, and `corrupt-workstream-index` explicit while the flow is waiting, regenerate `WORKSTREAMS.md` from canonical per-stream state under `.blueprint/workstreams/`, reject malformed canonical timestamps or snapshot content instead of coercing them, and use `blueprint_state_update` only for the final routing patch or returned resume snapshot. | `locked`; `source-owned`; `needs-behavior-audit` |
| `cleanup` | `src/mcp/command-runtime-metadata.ts#cleanup` | `blueprint-maintenance` | `blueprint_project_status`<br>`blueprint_roadmap_read`<br>`blueprint_artifact_list`<br>`blueprint_artifact_summary_digest`<br>`blueprint_artifact_report_write`<br>`blueprint_state_update` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | High-risk-maintenance profile for protected-scope phase-directory archival: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, keep the current phase, active roadmap references, evidence-incomplete directories, and final protected exclusions explicit before approval, surface preflight blockers as `dirty-working-tree`, `missing-phase-root`, or `inconsistent-phase-layout`, keep `cleanup-confirmation`, `archive-destination-confirmation`, and `report-overwrite-confirmation` visible while the flow is waiting, persist `cleanup-latest` before filesystem mutation, and stop instead of inventing an unsafe archive destination or archiving active-scope directories. | `locked`; `source-owned`; `needs-behavior-audit` |
| `update` | `src/mcp/command-runtime-metadata.ts#update` | `blueprint-maintenance` | `blueprint_update_check`<br>`blueprint_update_plan` | none | none | Interactive-read advisory profile for out-of-band extension updates: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, use Gemini-native `ask_user` only for the saved-checklist versus manual-fallback mode gate, keep extension-path handling read-only, persist only host-global metadata under `~/.<host>/blueprint/updates/`, and always end with restart guidance instead of implying an in-session self-update path. | `locked`; `source-owned`; `needs-behavior-audit` |
| `reapply-patches` | `src/mcp/command-runtime-metadata.ts#reapply-patches` | `blueprint-maintenance` | `blueprint_patch_list`<br>`blueprint_patch_reapply`<br>`blueprint_patch_record` | none | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | High-risk-maintenance profile for confirmation-gated patch replay: keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, hard-stop on `dirty-working-tree`, `malformed-patch-registry`, `missing-patch-target`, `compatibility-mismatch`, or `installed-extension-target`, preserve the flow `preflight -> preview -> confirm -> replay -> record`, and keep patch state under `~/.<host>/blueprint/patches/` instead of `.blueprint/reports/` or the installed extension copy. | `locked`; `source-owned`; `needs-behavior-audit` |

### Wave 3: Capture and lightweight execution (continued)

| Blueprint command | Command spec | Exact primary skill | Exact MCP destination | Optional agents | Hook involvement | Blueprint contract notes | Evidence state |
|---|---|---|---|---|---|---|---|
| `debug` | `docs/commands/debug.md` | `blueprint-debug` | `blueprint_project_status`<br>`blueprint_config_get`<br>`blueprint_artifact_report_write`<br>`blueprint_artifact_mutate_index`<br>`blueprint_state_update` | `blueprint-debugger` | `read-before-edit`; `.blueprint` write guard; `workflow advisory` | Interactive-read profile for evidence-backed investigations that can stay concise. Long-running-mutation profile for non-trivial investigations; keep Resolve/Read/Decide/Execute/Persist/Validate/Route narration plus resolved scope, active stage, pending gate, execution mode, and next safe action visible, read `blueprint_config_get` with `scope: "effective"` before any optional debugger decision, treat Gemini-native topic and todo tools as session-local visibility only, persist a durable `debug-latest` report, require an explicit follow-up gate before todo capture or fix attempts, keep `--diagnose` honest as diagnose-only until a fix attempt is approved, and route broader recovery into implemented follow-up commands instead of inventing hidden debug state. | `locked`; `docs-aligned`; `needs-behavior-audit` |

## Hook Surface Matrix

| Hook surface | Blueprint disposition | Why | Runtime location | Evidence state |
|---|---|---|---|---|
| `read-before-edit` advisory | Retain as an advisory Blueprint hook. | Reminds the model to read an existing repo file before editing it and reduces edit-loop mistakes without becoming a hidden executor. | `src/hooks/` bundled to `hooks/`, configured through `hooks/hooks.json` | `locked` |
| `.blueprint` write guard | Retain as an advisory Blueprint hook. | Warns on prompt-injection patterns or malformed `.blueprint/` artifact writes and pairs with MCP validation rather than raw shell writes. | `src/hooks/` bundled to `hooks/`, configured through `hooks/hooks.json` | `locked` |
| `workflow advisory` | Retain as an advisory Blueprint hook. | Warns when repo files are edited directly in a Blueprint-managed session outside the intended command flow; it suggests the right command instead of blocking. | `src/hooks/` bundled to `hooks/`, configured through `hooks/hooks.json` | `locked` |
| statusline hook | Explicitly omit. | Blueprint v1 does not depend on statusline injection or a statusline bridge. | not ported | `locked` |
| context-bridge or statusline-fed warning system | Explicitly omit. | Blueprint v1 keeps warning surfaces explicit and does not rely on hidden context-bridge wiring. | not ported | `locked` |
| extension self-update hooks | Explicitly omit. | `/blu-update` is advisory and Blueprint must not self-mutate the installed extension from inside a running session. | not ported | `locked` |
| installer-managed settings rewrites | Explicitly omit. | Blueprint is installed as a Gemini CLI or Tabnine CLI extension from GitHub, not through a custom installer that rewrites runtime settings. | not ported | `locked` |

## Open Verification Queue


### Highest-value behavior audits

- `validate-phase` and `verify-work`: confirm the exact validation and resumable UAT boundaries now that the summary-aware flow is shipped.
- `quick`: confirm the fast-path boundaries, especially discuss, research, and validation shortcuts.
- `audit-fix`: verify the exact review classification, remediation sequencing, and verification loop.
- `ship`: verify PR creation, review gating, and manual fallback behavior for git and remote operations.
- `undo`: verify the revert and dependency-check contract before implementation deepens.
- `remove-workspace`: confirm safety prompts, registry assumptions, and teardown boundaries before implementation.

### Hook behaviors to verify later

- Keep the stdin/stdout fixture contract stable for the three shipped advisory hooks under `src/hooks/` and `hooks/`.
- Confirm there are no hidden hook-like behaviors in retained workflows that should instead become skills, MCP tools, or explicit omissions in Blueprint.
- Re-check high-risk commands that mutate repo files so hook coverage stays aligned with the final implementation boundaries and does not drift into a hidden execution engine.
