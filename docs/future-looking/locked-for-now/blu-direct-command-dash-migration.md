# Direct Command Migration History

- Blueprint completed the direct-command migration from `/blu:<command>` to `/blu-<command>`.
- The one-release compatibility window has ended; colon-form direct commands are no longer supported.
- The supported command surfaces are now the `/blu` root router, direct `/blu-<command>` entrypoints, and router-style `/blu <command>` aliases.
