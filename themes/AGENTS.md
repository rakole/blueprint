# Blueprint Theme Instructions

Theme work should stay aligned with the extension manifest and the current visual direction.

## Rules

- Treat `gemini-extension.json` as the current theme source of truth.
- If richer theme assets are added here later, keep names aligned with manifest theme names.
- Update docs when a theme stops being defined entirely inline.
- Prefer small, intentional palette changes over broad restyling churn.

