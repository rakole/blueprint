# Themes Placeholder

Blueprint currently defines its starter theme inline in `gemini-extension.json`.

Use this directory later if we decide to keep richer theme assets or documentation alongside the extension.
