# Blueprint MCP Instructions

This directory is reserved for future MCP server work.

## Rules

- Do not add placeholder MCP code just to fill the directory.
- Only introduce server code when there is a concrete extension feature that needs it.
- Keep tool scope narrow and documentation explicit.
- When MCP support is added, update `gemini-extension.json`, docs, and policies together.
- Default to conservative permission assumptions until real tools prove otherwise.

