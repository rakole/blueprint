[project]
name = "harborlog"
version = "0.1.0"
description = "Synthetic standard-library-only harbor inspection fixture"
requires-python = ">=3.11"
dependencies = []

[tool.harborlog]
default-config = "config/default.toml"
