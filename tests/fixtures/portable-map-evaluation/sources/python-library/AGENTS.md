# Fixture development notes

Keep this synthetic library self-contained on Python standard-library runtime APIs.
Run focused tests with bytecode generation disabled and preserve the existing source,
configuration, CLI, serialization, repository, service, reporting, and validation
boundaries. Do not add generated outputs or runtime dependencies.
