# Field notes

Dates use ISO-8601 calendar dates in UTC. IDs are opaque strings generated by
the owning component. A dispatch is assigned to one depot and one route date;
the fulfillment service owns status transitions while the route planner owns
ordering and capacity estimates.
