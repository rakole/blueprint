---
phase: 03-phase-discovery
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - agents/blueprint-ui-designer.md
  - commands/blu/ui-phase.toml
  - skills/blueprint-phase-discovery.md
  - docs/COMMAND-CATALOG.md
  - docs/SKILLS-AND-AGENTS.md
  - tests/phase-discovery-ui.test.ts
  - tests/command-catalog.test.ts
  - tests/command-contract-docs.test.ts
autonomous: true
requirements:
  - LIFE-03
  - LIFE-01
  - LIFE-02
user_setup: []
must_haves:
  truths:
    - `ui-phase` uses `XX-UI-SPEC.md` as the sole phase-scoped durable output, whether it contains a UI contract or an explicit UI-skip rationale.
    - UI behavior is config-aware and honors `workflow.ui_phase` and `workflow.ui_safety_gate`.
    - Runtime exposure changes are limited to shipped Phase 3 discovery commands; later commands remain blocked.
  artifacts:
    - commands/blu/ui-phase.toml
    - tests/phase-discovery-ui.test.ts
    - docs/COMMAND-CATALOG.md
    - tests/command-catalog.test.ts
  key_links:
    - `docs/commands/ui-phase.md` defines required tools and frontend edge-case handling.
    - `docs/COMMAND-CATALOG.md` plus `tests/command-catalog.test.ts` lock runtime exposure truth.
    - `docs/SKILLS-AND-AGENTS.md` and `tests/command-contract-docs.test.ts` must stay metadata-consistent.
---

<objective>
Ship `ui-phase` and finalize Phase 3 discovery command exposure with regression protections.

Purpose: satisfy LIFE-03 while preserving contract consistency and runtime-safe command exposure boundaries.
Output: ui-phase command manifest and tests, config-aware UI-safety integration, and command-catalog/doc metadata updates that mark only shipped Phase 3 discovery commands implemented.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/DECISIONS.md
@docs/DRIFT.MD
@docs/ARCHITECTURE.md
@docs/MCP-TOOLS.md
@docs/COMMAND-CATALOG.md
@docs/SKILLS-AND-AGENTS.md
@docs/commands/ui-phase.md
@commands/blu/discuss-phase.toml
@commands/blu/research-phase.toml
@skills/blueprint-phase-discovery.md
@src/mcp/tools/config.ts
@src/mcp/tools/phase.ts
@tests/command-catalog.test.ts
@tests/command-contract-docs.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ui-phase command contract with config-aware UI safety flow</name>
  <files>agents/blueprint-ui-designer.md, commands/blu/ui-phase.toml, skills/blueprint-phase-discovery.md, tests/phase-discovery-ui.test.ts</files>
  <read_first>docs/commands/ui-phase.md, skills/blueprint-phase-discovery.md, src/mcp/tools/config.ts, src/mcp/tools/phase.ts, commands/blu/research-phase.toml</read_first>
  <action>Create `commands/blu/ui-phase.toml` with thin orchestration over `blueprint_phase_locate`, `blueprint_phase_research_status`, `blueprint_config_get`, `blueprint_artifact_scaffold`, and `blueprint_state_update`. Add `agents/blueprint-ui-designer.md` so the documented bounded-agent contract exists when the command ships. Update `skills/blueprint-phase-discovery.md` UI section so frontend detection and skip-rationale handling are explicit, ensure `workflow.ui_phase` plus `workflow.ui_safety_gate` from effective config drive behavior, and keep `XX-UI-SPEC.md` as the single durable phase-scoped output for both UI-contract and skip-rationale branches. Add `tests/phase-discovery-ui.test.ts` to verify manifest tool parity, UI-skip prompt expectations, and the single-output write contract (`XX-UI-SPEC.md`).</action>
  <verify>
    <automated>npm run build --silent && tsx --test tests/phase-discovery-ui.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `commands/blu/ui-phase.toml` exists and references only registered required tools
    - `agents/blueprint-ui-designer.md` exists and matches the documented bounded-agent expectation
    - ui-phase flow documents both UI-spec generation and explicit skip-rationale branch
    - config-aware behavior references effective config keys (`workflow.ui_phase`, `workflow.ui_safety_gate`)
    - ui-phase flow does not invent a second phase artifact for skip handling
    - ui-phase tests pass and cover frontend and backend-only phase scenarios
  </acceptance_criteria>
  <done>UI phase discovery behavior is implemented with safe config-aware gating and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize discovery command exposure and lock metadata consistency</name>
  <files>docs/COMMAND-CATALOG.md, docs/SKILLS-AND-AGENTS.md, tests/command-catalog.test.ts, tests/command-contract-docs.test.ts</files>
  <read_first>docs/COMMAND-CATALOG.md, docs/SKILLS-AND-AGENTS.md, docs/commands/discuss-phase.md, docs/commands/research-phase.md, docs/commands/ui-phase.md, tests/command-catalog.test.ts, tests/command-contract-docs.test.ts</read_first>
  <action>Update `docs/COMMAND-CATALOG.md` and `docs/SKILLS-AND-AGENTS.md` so `ui-phase` becomes implemented at the command level and the shared `blueprint-phase-discovery` skill-family status flips only now that the runtime skill file and the three core discovery commands are all present. Keep `list-phase-assumptions` non-runnable at the command level. Update `tests/command-catalog.test.ts` expected implemented command set to include only `discuss-phase`, `research-phase`, and `ui-phase` in addition to existing Wave 0 commands, and extend catalog coverage for the newly available discovery optional-agent contracts. Extend `tests/command-contract-docs.test.ts` assertions so command metadata parity remains strict after these status changes.</action>
  <verify>
    <automated>npm run build --silent && tsx --test tests/command-catalog.test.ts tests/command-contract-docs.test.ts tests/phase-discovery-tools.test.ts tests/phase-discovery-discuss.test.ts tests/phase-discovery-research.test.ts tests/phase-discovery-ui.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - docs and tests agree on exactly which discovery commands are implemented
    - the shared discovery-skill status can be implemented without implying `list-phase-assumptions` is runnable
    - runtime catalog tests confirm later lifecycle commands remain blocked until their substrate lands
    - full test suite passes after discovery command exposure updates
  </acceptance_criteria>
  <done>Phase 3 discovery commands are exposed safely with contract drift regression locks in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI heuristics/config -> artifact writes | Weak gating can create wrong UI artifacts or skip rationale without traceability |
| catalog/docs metadata -> runtime guidance | Drift can expose non-shipped commands or hide shipped ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | `commands/blu/ui-phase.toml` UI-gate flow | mitigate | Tie gate behavior to effective config and test both UI and non-UI branches |
| T-03-03-02 | I | UI skip rationale handling | mitigate | Require explicit rationale text and deterministic artifact/state recording |
| T-03-03-03 | T | `docs/COMMAND-CATALOG.md` and catalog tests | mitigate | Update metadata and regression expectations together so non-shipped commands remain blocked |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run build --silent` passes
- [ ] `tsx --test tests/phase-discovery-ui.test.ts tests/command-catalog.test.ts tests/command-contract-docs.test.ts` passes
- [ ] `npm test` passes
- [ ] `rg -n "discuss-phase|research-phase|ui-phase" docs/COMMAND-CATALOG.md docs/SKILLS-AND-AGENTS.md tests/command-catalog.test.ts` shows consistent implemented metadata
</verification>

<success_criteria>

- All tasks completed
- `/blu:ui-phase` supports UI-spec and explicit skip-rationale flows with config-aware gating
- command catalog/doc metadata and regression tests are consistent
- the shared discovery-skill rollout is complete without elevating `list-phase-assumptions`
- LIFE-03 is satisfied without exposing non-shipped commands

</success_criteria>

<output>
After completion, create `.planning/phases/03-phase-discovery/03-03-SUMMARY.md`
</output>
