---
phase: 03-phase-discovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/phase.ts
  - src/mcp/server.ts
  - src/mcp/tools/artifacts.ts
  - commands/blu/discuss-phase.toml
  - skills/blueprint-phase-discovery.md
  - tests/phase-discovery-tools.test.ts
  - tests/phase-discovery-discuss.test.ts
autonomous: true
requirements:
  - LIFE-01
user_setup: []
must_haves:
  truths:
    - Phase lookup, context summary, and roadmap reads are MCP-owned and deterministic.
    - `discuss-phase` writes only phase-scoped context artifacts with explicit overwrite confirmation.
    - Command logic stays thin while persistence and path safety remain in MCP tools.
  artifacts:
    - src/mcp/tools/phase.ts
    - commands/blu/discuss-phase.toml
    - skills/blueprint-phase-discovery.md
    - tests/phase-discovery-tools.test.ts
    - tests/phase-discovery-discuss.test.ts
  key_links:
    - `docs/commands/discuss-phase.md` and `docs/MCP-TOOLS.md` define required tool contracts and write targets.
    - `src/mcp/server.ts` must register new phase tools so command manifests can reference runtime-available names.
    - `src/mcp/tools/artifacts.ts` remains the shared scaffold owner for deterministic artifact creation.
---

<objective>
Ship the Phase 3 substrate and `discuss-phase` command path needed to persist context artifacts safely.

Purpose: satisfy LIFE-01 by introducing phase-aware MCP primitives, wiring command registration, and making `/blu:discuss-phase` executable through Blueprint's thin-command/skill-led architecture.
Output: registered phase MCP tools, discuss-phase command manifest, discovery skill contract, and focused regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/DECISIONS.md
@docs/DRIFT.MD
@docs/ARCHITECTURE.md
@docs/ARTIFACT-SCHEMA.md
@docs/MCP-TOOLS.md
@docs/PHASE-LIFECYCLE.md
@docs/IMPLEMENTATION-ORDER.md
@docs/COMMAND-CATALOG.md
@docs/commands/discuss-phase.md
@src/mcp/server.ts
@src/mcp/tools/project.ts
@src/mcp/tools/artifacts.ts
@src/mcp/tools/state.ts
@tests/command-catalog.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the phase MCP tool family and phase artifact scaffold support</name>
  <files>src/mcp/tools/phase.ts, src/mcp/server.ts, src/mcp/tools/artifacts.ts, tests/phase-discovery-tools.test.ts</files>
  <read_first>src/mcp/server.ts, src/mcp/tools/project.ts, src/mcp/tools/artifacts.ts, src/mcp/tools/state.ts, docs/MCP-TOOLS.md, docs/commands/discuss-phase.md, docs/ARTIFACT-SCHEMA.md</read_first>
  <action>Create `src/mcp/tools/phase.ts` with `blueprint_phase_locate`, `blueprint_phase_context`, `blueprint_roadmap_read`, and `blueprint_phase_research_status` using zod-validated args and path-safe resolution rooted at `.blueprint/`. Register all new tools in `src/mcp/server.ts` and keep existing required Wave 0 registrations unchanged. Extend scaffold logic in `src/mcp/tools/artifacts.ts` to support deterministic phase artifact templates for `XX-CONTEXT.md`, `XX-DISCUSSION-LOG.md`, `XX-RESEARCH.md`, and `XX-UI-SPEC.md` without enabling path traversal or non-phase writes. Add `tests/phase-discovery-tools.test.ts` fixture-driven coverage for valid phase lookup, missing-phase behavior, and artifact-presence status reporting.</action>
  <verify>
    <automated>npm run build --silent && tsx --test tests/phase-discovery-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/tools/phase.ts` exports all four required tool definitions with documented return fields
    - `src/mcp/server.ts` registers the new phase tool definitions
    - phase tool paths stay under `.blueprint/` and reject traversal attempts
    - `tests/phase-discovery-tools.test.ts` passes and validates success plus failure paths
  </acceptance_criteria>
  <done>Phase tool substrate is available and safely registered for discovery commands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement discuss-phase command contract and discovery skill flow</name>
  <files>commands/blu/discuss-phase.toml, skills/blueprint-phase-discovery.md, tests/phase-discovery-discuss.test.ts</files>
  <read_first>docs/commands/discuss-phase.md, commands/blu/help.toml, commands/blu/progress.toml, skills/blueprint-router.md, skills/blueprint-governance.md, tests/new-project.test.ts, tests/help-progress-health.test.ts</read_first>
  <action>Create `commands/blu/discuss-phase.toml` with thin command instructions that call `blueprint_phase_locate`, `blueprint_phase_context`, `blueprint_roadmap_read`, `blueprint_artifact_list`, `blueprint_artifact_scaffold`, and `blueprint_state_update`, including explicit overwrite confirmation when context artifacts already exist. Create `skills/blueprint-phase-discovery.md` with separate sections for discuss/research/ui orchestration and explicit rule that all writes use MCP tools only. Add `tests/phase-discovery-discuss.test.ts` to assert manifest tool-name parity with MCP registrations and to validate discuss-flow artifact creation behavior in fixtures.</action>
  <verify>
    <automated>npm run build --silent && tsx --test tests/phase-discovery-discuss.test.ts tests/phase-discovery-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `commands/blu/discuss-phase.toml` exists and references only registered MCP tool names
    - `skills/blueprint-phase-discovery.md` includes discuss-phase orchestration rules and MCP-owned write constraints
    - discuss-phase tests pass and cover overwrite-confirmation contract text
    - no non-phase repo files are added to discuss-phase declared write scope
  </acceptance_criteria>
  <done>Discuss-phase runtime contract is executable and LIFE-01 behavior is covered by regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command prompt -> MCP tools | If command logic bypasses MCP, phase artifact writes can become nondeterministic or unsafe |
| phase input -> filesystem paths | Incorrect phase resolution could target wrong directories or allow traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | `src/mcp/tools/phase.ts` phase resolution | mitigate | Validate phase inputs against roadmap data and enforce `.blueprint/phases/` rooted writes only |
| T-03-01-02 | E | `commands/blu/discuss-phase.toml` overwrite path | mitigate | Require explicit confirmation before replacing existing context artifacts |
| T-03-01-03 | R | phase context artifact provenance | mitigate | Persist state updates with clear `activeCommand`/`nextAction` after discuss completion |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run build --silent` passes
- [ ] `tsx --test tests/phase-discovery-tools.test.ts tests/phase-discovery-discuss.test.ts` passes
- [ ] `rg -n "blueprint_phase_locate|blueprint_phase_context|blueprint_roadmap_read|blueprint_phase_research_status" src/mcp/server.ts commands/blu/discuss-phase.toml` confirms tool registration and manifest parity
</verification>

<success_criteria>

- All tasks completed
- Phase MCP substrate is registered and deterministic
- `/blu:discuss-phase` contract exists and writes only declared phase artifacts
- LIFE-01 is satisfied with automated regression coverage

</success_criteria>

<output>
After completion, create `.planning/phases/03-phase-discovery/03-01-SUMMARY.md`
</output>
