---
phase: 03-phase-discovery
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - commands/blu/research-phase.toml
  - skills/blueprint-phase-discovery.md
  - tests/phase-discovery-research.test.ts
  - tests/phase-discovery-tools.test.ts
autonomous: true
requirements:
  - LIFE-02
user_setup: []
must_haves:
  truths:
    - `research-phase` uses phase-location and context MCP tools instead of prompt-only file writes.
    - Research writes remain phase-scoped and update state for next-step routing.
    - Existing research artifacts trigger explicit replacement confirmation.
  artifacts:
    - commands/blu/research-phase.toml
    - skills/blueprint-phase-discovery.md
    - tests/phase-discovery-research.test.ts
  key_links:
    - `docs/commands/research-phase.md` defines required tools and low-risk write contract.
    - `docs/PHASE-LIFECYCLE.md` requires research as a durable pre-planning artifact.
    - `tests/command-catalog.test.ts` should not mark non-shipped later commands implemented.
---

<objective>
Implement `research-phase` orchestration so users can run targeted phase research with durable artifact output.

Purpose: satisfy LIFE-02 by shipping `/blu:research-phase` on top of phase MCP substrate from plan 03-01, including safe replacement flow and deterministic state updates.
Output: research-phase command manifest, discovery-skill research branch, and regression tests for readiness and overwrite behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/DECISIONS.md
@docs/ARCHITECTURE.md
@docs/MCP-TOOLS.md
@docs/PHASE-LIFECYCLE.md
@docs/commands/research-phase.md
@skills/blueprint-phase-discovery.md
@commands/blu/discuss-phase.toml
@src/mcp/tools/phase.ts
@src/mcp/tools/state.ts
@tests/phase-discovery-tools.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship research-phase command manifest and skill branch</name>
  <files>commands/blu/research-phase.toml, skills/blueprint-phase-discovery.md</files>
  <read_first>docs/commands/research-phase.md, skills/blueprint-phase-discovery.md, commands/blu/discuss-phase.toml, commands/blu/map-codebase.toml, docs/ARCHITECTURE.md</read_first>
  <action>Create `commands/blu/research-phase.toml` with thin command behavior that calls `blueprint_phase_locate`, `blueprint_phase_context`, `blueprint_phase_research_status`, `blueprint_artifact_scaffold`, and `blueprint_state_update`. Update `skills/blueprint-phase-discovery.md` research section to specify the standalone research path, explicit overwrite confirmation when `XX-RESEARCH.md` already exists, and deterministic next-step routing toward `ui-phase` or `plan-phase` depending on phase needs.</action>
  <verify>
    <automated>npm run build --silent && tsx --test tests/phase-discovery-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `commands/blu/research-phase.toml` exists and references only runtime-registered tools
    - research-phase manifest includes overwrite confirmation behavior for existing research artifacts
    - skill section describes research-phase flow without direct filesystem mutation steps
  </acceptance_criteria>
  <done>Research-phase command contract is present and aligned with Blueprint architecture constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Add research-phase regression tests for readiness and safe replacement</name>
  <files>tests/phase-discovery-research.test.ts, tests/phase-discovery-tools.test.ts</files>
  <read_first>tests/new-project.test.ts, tests/help-progress-health.test.ts, tests/phase-discovery-tools.test.ts, commands/blu/research-phase.toml, src/mcp/tools/phase.ts</read_first>
  <action>Add `tests/phase-discovery-research.test.ts` to validate command manifest tool parity, research-status return shape, and replacement-safety prompt requirements. Extend `tests/phase-discovery-tools.test.ts` with targeted checks that `blueprint_phase_research_status` correctly reflects combinations of context/research/ui-spec artifact presence. Keep fixtures deterministic and scoped to temporary repo copies under `tests/fixtures/`.</action>
  <verify>
    <automated>npm run build --silent && tsx --test tests/phase-discovery-tools.test.ts tests/phase-discovery-research.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - research-phase tests pass and cover both first-write and overwrite-confirmation paths
    - research-status tests assert `hasContext`, `hasResearch`, and `hasUiSpec` values for multiple fixture states
    - no test relies on external network or mutable global user state
  </acceptance_criteria>
  <done>LIFE-02 behavior is covered by deterministic command-contract and tool regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research readiness signals -> command routing | Incorrect status can route users to wrong next step or overwrite artifacts unexpectedly |
| command prompts -> user confirmations | Missing replacement gate can destroy existing research artifacts silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T | `blueprint_phase_research_status` output | mitigate | Test artifact-presence permutations and enforce stable boolean contract |
| T-03-02-02 | E | `commands/blu/research-phase.toml` overwrite path | mitigate | Require explicit confirmation text before replacement write calls |
| T-03-02-03 | I | research artifact handling | mitigate | Restrict writes to selected phase directory and avoid cross-phase reads for persistence |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm run build --silent` passes
- [ ] `tsx --test tests/phase-discovery-tools.test.ts tests/phase-discovery-research.test.ts` passes
- [ ] `rg -n "blueprint_phase_locate|blueprint_phase_context|blueprint_phase_research_status|blueprint_artifact_scaffold|blueprint_state_update" commands/blu/research-phase.toml` confirms command/tool contract parity
</verification>

<success_criteria>

- All tasks completed
- `/blu:research-phase` behavior is deterministic and phase-scoped
- Overwrite flow is explicitly confirmed and tested
- LIFE-02 is satisfied with automated coverage

</success_criteria>

<output>
After completion, create `.planning/phases/03-phase-discovery/03-02-SUMMARY.md`
</output>
