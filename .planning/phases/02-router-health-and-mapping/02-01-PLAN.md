---
phase: 02-router-health-and-mapping
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/blu/settings.toml
  - commands/blu/set-profile.toml
  - src/mcp/server.ts
  - src/mcp/tools/config.ts
  - tests/settings-profile.test.ts
  - tests/fixtures/settings-profile/
autonomous: true
requirements:
  - FND-04
user_setup: []
must_haves:
  truths:
    - The extension exposes `/blu:settings` and `/blu:set-profile` as direct commands that use only documented config/project MCP tools.
    - `blueprint_config_set` can normalize legacy or minimal config into the full v2 shape while rejecting repo-level `hooks.*`, `workflow.use_workspaces`, and `workflow.use_workstreams`.
    - `blueprint_config_set_profile` updates only `model_profile` in `.blueprint/config.json` and never writes `~/.gemini/blueprint/defaults.json`.
  artifacts:
    - commands/blu/settings.toml
    - commands/blu/set-profile.toml
    - src/mcp/server.ts
    - src/mcp/tools/config.ts
    - tests/settings-profile.test.ts
  key_links:
    - `commands/blu/settings.toml` references `blueprint_project_status`, `blueprint_config_get`, and `blueprint_config_set`.
    - `commands/blu/set-profile.toml` references `blueprint_config_get` and `blueprint_config_set_profile`.
    - `src/mcp/server.ts` registers `blueprint_config_set_profile` from `src/mcp/tools/config.ts`.
    - `tests/settings-profile.test.ts` proves profile-only mutation, config migration, and reserved-key rejection.
---

<objective>
Implement the Phase 2 governance/config slice for Wave 0.

Purpose: Let users change Blueprint repo settings and model profile without hand-editing raw JSON, while preserving the normalized config contract that later read-path commands depend on.
Output: `settings` and `set-profile` command contracts, the missing profile-specific MCP tool, and fixture-backed config migration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-router-health-and-mapping/02-CONTEXT.md
@.planning/phases/02-router-health-and-mapping/02-RESEARCH.md
@AGENTS.md
@docs/ARTIFACT-SCHEMA.md
@docs/MCP-TOOLS.md
@docs/commands/settings.md
@docs/commands/set-profile.md
@src/mcp/server.ts
@src/mcp/tools/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the config MCP surface for profile-only writes and legacy normalization</name>
  <files>src/mcp/tools/config.ts, src/mcp/server.ts</files>
  <read_first>AGENTS.md, docs/ARTIFACT-SCHEMA.md, docs/MCP-TOOLS.md, docs/commands/settings.md, docs/commands/set-profile.md, src/mcp/tools/config.ts, src/mcp/server.ts, .planning/phases/02-router-health-and-mapping/02-CONTEXT.md, .planning/phases/02-router-health-and-mapping/02-RESEARCH.md</read_first>
  <action>In `src/mcp/tools/config.ts`, add `blueprint_config_set_profile` with an input enum of `quality`, `balanced`, `budget`, and `inherit`. Make it load the current project config, write only `model_profile`, and return `{profile, updatedKeys, configPath}` with `updatedKeys` limited to `model_profile`. At the same time, tighten the existing config normalization path so sparse or legacy `.blueprint/config.json` files are upgraded to the full version 2 shape on read/write, malformed defaults still fall back to hardcoded defaults with warnings, and repo-level `hooks.*`, `workflow.use_workspaces`, and `workflow.use_workstreams` remain rejected. Register the new tool through `configToolDefinitions` so `src/mcp/server.ts` exposes `blueprint_config_set_profile`.</action>
  <verify>rg -n 'blueprint_config_set_profile|quality|balanced|budget|inherit|model_profile|workflow.use_workspaces|workflow.use_workstreams|hooks\\.' src/mcp/tools/config.ts src/mcp/server.ts</verify>
  <acceptance_criteria>
    - `src/mcp/tools/config.ts` contains `export async function blueprintConfigSetProfile`
    - `src/mcp/tools/config.ts` contains `name: "blueprint_config_set_profile"`
    - `src/mcp/tools/config.ts` contains all four profile values: `quality`, `balanced`, `budget`, and `inherit`
    - `src/mcp/tools/config.ts` returns `updatedKeys` for `model_profile`
    - `src/mcp/tools/config.ts` still rejects `workflow.use_workspaces`, `workflow.use_workstreams`, and `hooks`
    - `src/mcp/server.ts` contains `blueprint_config_set_profile`
  </acceptance_criteria>
  <done>The config tool layer supports both broad settings updates and project-local profile changes without scope leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Add direct command contracts for `settings` and `set-profile`</name>
  <files>commands/blu/settings.toml, commands/blu/set-profile.toml</files>
  <read_first>docs/commands/settings.md, docs/commands/set-profile.md, docs/MCP-TOOLS.md, GEMINI.md, commands/blu/new-project.toml, commands/blu/settings.toml, commands/blu/set-profile.toml, src/mcp/server.ts, src/mcp/tools/config.ts</read_first>
  <action>Create `commands/blu/settings.toml` so it requires an initialized Blueprint project, reads status through `blueprint_project_status`, loads current config through `blueprint_config_get`, writes repo or saved-default changes through `blueprint_config_set`, runs a common settings pass for `model_profile`, research/plan/verify toggles, Nyquist, UI, code review, `planning.commit_docs`, git branching, and `workflow.use_worktrees`, then offers an advanced pass for gates, safety, timeouts, response language, templates, and `agent_skills`. Create `commands/blu/set-profile.toml` so it accepts `<profile (quality|balanced|budget|inherit)>`, shows the old and new profile, reads the current value through `blueprint_config_get`, writes only through `blueprint_config_set_profile`, and explicitly says it must not mutate `~/.gemini/blueprint/defaults.json`.</action>
  <verify>rg -n 'blueprint_project_status|blueprint_config_get|blueprint_config_set|blueprint_config_set_profile|defaults.json|quality|balanced|budget|inherit|use_worktrees|commit_docs' commands/blu/settings.toml commands/blu/set-profile.toml</verify>
  <acceptance_criteria>
    - `commands/blu/settings.toml` exists
    - `commands/blu/settings.toml` contains `blueprint_project_status`
    - `commands/blu/settings.toml` contains `blueprint_config_get`
    - `commands/blu/settings.toml` contains `blueprint_config_set`
    - `commands/blu/settings.toml` contains `commit_docs` and `use_worktrees`
    - `commands/blu/set-profile.toml` exists
    - `commands/blu/set-profile.toml` contains `blueprint_config_get`
    - `commands/blu/set-profile.toml` contains `blueprint_config_set_profile`
    - `commands/blu/set-profile.toml` contains `quality|balanced|budget|inherit`
    - `commands/blu/set-profile.toml` contains `defaults.json`
  </acceptance_criteria>
  <done>The governance command layer exists as explicit Gemini command contracts over the documented config tools.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture-backed tests for settings mutation, profile isolation, and config migration</name>
  <files>tests/settings-profile.test.ts, tests/fixtures/settings-profile/</files>
  <read_first>AGENTS.md, docs/ARTIFACT-SCHEMA.md, docs/commands/settings.md, docs/commands/set-profile.md, src/mcp/server.ts, src/mcp/tools/config.ts, tests/settings-profile.test.ts, tests/new-project.test.ts, tests/phase-01-validation.test.ts, package.json</read_first>
  <action>Create `tests/settings-profile.test.ts` plus fixtures under `tests/fixtures/settings-profile/` for an initialized repo, a legacy/minimal-config repo, and any saved-default inputs the plan needs. Reuse the existing temp-repo fixture pattern. Assert that `blueprint_config_set` persists normalized version 2 config, `blueprint_config_set_profile` changes only `model_profile`, saved defaults remain unchanged during a profile switch, reserved repo keys are rejected, legacy/minimal config is upgraded to the full schema on write, and both new command TOMLs reference the same tool names that `src/mcp/server.ts` registers.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `tests/settings-profile.test.ts` exists
    - `tests/settings-profile.test.ts` asserts `"version": 2`
    - `tests/settings-profile.test.ts` asserts `model_profile`
    - `tests/settings-profile.test.ts` asserts saved defaults stay unchanged during `set-profile`
    - `tests/settings-profile.test.ts` asserts rejection of `workflow.use_workspaces`, `workflow.use_workstreams`, or `hooks`
    - `tests/settings-profile.test.ts` asserts `commands/blu/settings.toml` and `commands/blu/set-profile.toml` reference the registered tool names
  </acceptance_criteria>
  <done>Phase 2 config behavior is covered by deterministic tests for the happy path, migration path, and the highest-risk scope boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] `rg -n 'blueprint_config_set_profile|commands/blu/settings.toml|commands/blu/set-profile.toml|model_profile' src/mcp/tools/config.ts src/mcp/server.ts tests/settings-profile.test.ts` finds the expected strings
</verification>

<success_criteria>

- All tasks completed
- Users can change Blueprint repo settings without hand-editing JSON
- Users can switch project model profile without mutating saved defaults
- Config migration and reserved-key guardrails are enforced by tests

</success_criteria>

<output>
After completion, create `.planning/phases/02-router-health-and-mapping/02-01-SUMMARY.md`
</output>
