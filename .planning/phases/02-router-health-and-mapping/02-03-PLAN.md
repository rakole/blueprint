---
phase: 02-router-health-and-mapping
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - commands/blu/map-codebase.toml
  - src/mcp/server.ts
  - src/mcp/tools/artifacts.ts
  - tests/map-codebase.test.ts
  - tests/fixtures/map-codebase/
autonomous: true
requirements:
  - FND-06
user_setup: []
must_haves:
  truths:
    - `map-codebase` writes the locked six-file `.blueprint/codebase/` bundle and does not reintroduce `.planning/codebase/`.
    - The artifact tool family can scaffold, list, validate, and summarize codebase mapping outputs deterministically from repo evidence.
    - Existing edited codebase mapping docs are preserved unless the command takes an explicit replace path.
  artifacts:
    - commands/blu/map-codebase.toml
    - src/mcp/server.ts
    - src/mcp/tools/artifacts.ts
    - tests/map-codebase.test.ts
  key_links:
    - `commands/blu/map-codebase.toml` references `blueprint_project_status`, `blueprint_artifact_scaffold`, `blueprint_artifact_list`, and `blueprint_artifact_summary_digest`.
    - `src/mcp/tools/artifacts.ts` contains the six `.blueprint/codebase/*.md` filenames and `blueprint_artifact_summary_digest`.
    - `tests/map-codebase.test.ts` proves deterministic codebase scaffolding, digest output, and overwrite/reuse behavior.
---

<objective>
Implement deterministic brownfield codebase mapping for Wave 0.

Purpose: Give initialized Blueprint repos a stable way to summarize an existing codebase into the six artifact names that later discovery and planning commands depend on.
Output: codebase artifact helpers, the `/blu:map-codebase` command contract, and brownfield mapping fixtures/tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-router-health-and-mapping/02-CONTEXT.md
@.planning/phases/02-router-health-and-mapping/02-RESEARCH.md
@AGENTS.md
@docs/ARTIFACT-SCHEMA.md
@docs/MCP-TOOLS.md
@docs/commands/map-codebase.md
@src/mcp/server.ts
@src/mcp/tools/artifacts.ts
@tests/new-project.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend artifact helpers for the `.blueprint/codebase/` bundle</name>
  <files>src/mcp/server.ts, src/mcp/tools/artifacts.ts</files>
  <read_first>AGENTS.md, docs/ARTIFACT-SCHEMA.md, docs/MCP-TOOLS.md, docs/commands/map-codebase.md, src/mcp/server.ts, src/mcp/tools/artifacts.ts, .planning/phases/02-router-health-and-mapping/02-RESEARCH.md</read_first>
  <action>Extend `src/mcp/tools/artifacts.ts` so `blueprint_artifact_scaffold` understands `.blueprint/codebase/STACK.md`, `.blueprint/codebase/ARCHITECTURE.md`, `.blueprint/codebase/CONVENTIONS.md`, `.blueprint/codebase/TESTING.md`, `.blueprint/codebase/INTEGRATIONS.md`, and `.blueprint/codebase/CONCERNS.md` in addition to the bootstrap files. Add `blueprint_artifact_summary_digest` that takes deterministic repo evidence inputs and returns `{digest, inputsUsed}` with one summary section per codebase artifact. Make sure existing `blueprint_artifact_list` and `blueprint_artifact_validate` logic recognizes the codebase bundle and surfaces overwrite/reuse warnings when those files already exist. Register `blueprint_artifact_summary_digest` in `src/mcp/server.ts`.</action>
  <verify>rg -n 'STACK.md|ARCHITECTURE.md|CONVENTIONS.md|TESTING.md|INTEGRATIONS.md|CONCERNS.md|blueprint_artifact_summary_digest|inputsUsed' src/mcp/server.ts src/mcp/tools/artifacts.ts</verify>
  <acceptance_criteria>
    - `src/mcp/tools/artifacts.ts` contains all six `.blueprint/codebase/*.md` filenames
    - `src/mcp/tools/artifacts.ts` contains `export async function blueprintArtifactSummaryDigest`
    - `src/mcp/tools/artifacts.ts` returns `digest` and `inputsUsed`
    - `src/mcp/server.ts` contains `blueprint_artifact_summary_digest`
    - `src/mcp/tools/artifacts.ts` surfaces overwrite or reuse warnings for existing codebase artifacts
  </acceptance_criteria>
  <done>The artifact tool layer can create and summarize the stable codebase mapping bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Create the `/blu:map-codebase` command contract</name>
  <files>commands/blu/map-codebase.toml</files>
  <read_first>docs/commands/map-codebase.md, docs/MCP-TOOLS.md, GEMINI.md, commands/blu.toml, commands/blu/map-codebase.toml, src/mcp/server.ts, src/mcp/tools/artifacts.ts</read_first>
  <action>Create `commands/blu/map-codebase.toml` so it requires an initialized Blueprint project, accepts an optional focus area argument, gathers repo evidence from `package.json`, source directories, tests, docs, and git-tracked files, calls `blueprint_project_status` for readiness, calls `blueprint_artifact_scaffold` to create or reuse the six `.blueprint/codebase/*.md` files, calls `blueprint_artifact_list` to inspect existing outputs, and calls `blueprint_artifact_summary_digest` to build the user-facing summaries. The prompt must confirm before replacing heavily edited codebase docs and end with a next-step recommendation toward discovery or planning.</action>
  <verify>rg -n 'blueprint_project_status|blueprint_artifact_scaffold|blueprint_artifact_list|blueprint_artifact_summary_digest|STACK.md|ARCHITECTURE.md|CONCERNS.md|git|rg' commands/blu/map-codebase.toml</verify>
  <acceptance_criteria>
    - `commands/blu/map-codebase.toml` exists
    - `commands/blu/map-codebase.toml` contains `blueprint_project_status`
    - `commands/blu/map-codebase.toml` contains `blueprint_artifact_scaffold`
    - `commands/blu/map-codebase.toml` contains `blueprint_artifact_list`
    - `commands/blu/map-codebase.toml` contains `blueprint_artifact_summary_digest`
    - `commands/blu/map-codebase.toml` contains the filenames `STACK.md`, `ARCHITECTURE.md`, and `CONCERNS.md`
    - `commands/blu/map-codebase.toml` contains `heavily edited`
    - `commands/blu/map-codebase.toml` contains `replace`
  </acceptance_criteria>
  <done>The brownfield mapping flow exists as an explicit command contract over the artifact tool family.</done>
</task>

<task type="auto">
  <name>Task 3: Add brownfield fixtures and tests for deterministic codebase mapping</name>
  <files>tests/map-codebase.test.ts, tests/fixtures/map-codebase/</files>
  <read_first>AGENTS.md, docs/ARTIFACT-SCHEMA.md, docs/commands/map-codebase.md, src/mcp/server.ts, src/mcp/tools/artifacts.ts, src/mcp/tools/project.ts, tests/map-codebase.test.ts, tests/new-project.test.ts, package.json</read_first>
  <action>Create `tests/map-codebase.test.ts` plus a brownfield fixture under `tests/fixtures/map-codebase/` that includes a representative `package.json`, `src/`, `tests/`, `docs/`, and README content. Use `blueprintProjectInit` to seed `.blueprint/` first, then assert that the codebase artifact bundle is created under `.blueprint/codebase/`, the digest output references concrete fixture evidence such as the runtime, test framework, docs, and integrations, existing codebase docs are reused or warned on rather than silently replaced, and `commands/blu/map-codebase.toml` references the same tool names registered in `src/mcp/server.ts`.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `tests/map-codebase.test.ts` exists
    - `tests/map-codebase.test.ts` asserts creation of `.blueprint/codebase/STACK.md`
    - `tests/map-codebase.test.ts` asserts creation of `.blueprint/codebase/ARCHITECTURE.md`
    - `tests/map-codebase.test.ts` asserts creation of `.blueprint/codebase/CONVENTIONS.md`
    - `tests/map-codebase.test.ts` asserts creation of `.blueprint/codebase/TESTING.md`
    - `tests/map-codebase.test.ts` asserts creation of `.blueprint/codebase/INTEGRATIONS.md`
    - `tests/map-codebase.test.ts` asserts creation of `.blueprint/codebase/CONCERNS.md`
    - `tests/map-codebase.test.ts` asserts digest output includes `TypeScript`, `node:test`, and `docs/`
    - `tests/map-codebase.test.ts` asserts reuse or warning behavior for pre-existing codebase docs
    - `tests/map-codebase.test.ts` asserts `commands/blu/map-codebase.toml` matches registered tool names
  </acceptance_criteria>
  <done>The Phase 2 brownfield mapping path is covered by deterministic fixture-driven tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] `rg -n 'blueprint_artifact_summary_digest|STACK.md|ARCHITECTURE.md|CONCERNS.md|map-codebase' src/mcp/tools/artifacts.ts commands/blu/map-codebase.toml tests/map-codebase.test.ts` finds the expected strings
</verification>

<success_criteria>

- All tasks completed
- Brownfield users can generate the six stable `.blueprint/codebase/` artifacts
- Mapping output is deterministic and protected against accidental overwrite
- The command contract and MCP tool registry stay aligned

</success_criteria>

<output>
After completion, create `.planning/phases/02-router-health-and-mapping/02-03-SUMMARY.md`
</output>
