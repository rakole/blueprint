---
phase: 02-router-health-and-mapping
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - GEMINI.md
  - README.md
  - commands/blu/help.toml
  - commands/blu/progress.toml
  - commands/blu/health.toml
  - src/mcp/server.ts
  - src/mcp/tools/project.ts
  - src/mcp/tools/state.ts
  - src/mcp/tools/artifacts.ts
  - tests/help-progress-health.test.ts
  - tests/fixtures/help-progress-health/
autonomous: true
requirements:
  - FND-05
user_setup: []
must_haves:
  truths:
    - The extension exposes `/blu:help`, `/blu:progress`, and `/blu:health` as direct commands, and `health` stays read-only unless repair is explicitly requested.
    - Shared MCP read-path helpers can distinguish uninitialized, partial, and initialized Blueprint repos without prompt-only heuristics.
    - Runtime-facing docs describe the shipped Wave 0 command surface accurately and keep `.planning/` out of product runtime behavior.
  artifacts:
    - GEMINI.md
    - README.md
    - commands/blu/help.toml
    - commands/blu/progress.toml
    - commands/blu/health.toml
    - src/mcp/server.ts
    - src/mcp/tools/project.ts
    - src/mcp/tools/state.ts
    - src/mcp/tools/artifacts.ts
    - tests/help-progress-health.test.ts
  key_links:
    - `commands/blu/help.toml` references `blueprint_command_catalog` and `blueprint_project_status`.
    - `commands/blu/progress.toml` references `blueprint_project_status`, `blueprint_config_get`, `blueprint_state_load`, `blueprint_artifact_list`, and `blueprint_command_catalog`.
    - `commands/blu/health.toml` references `blueprint_project_status`, `blueprint_config_get`, `blueprint_config_set`, `blueprint_state_load`, `blueprint_artifact_list`, `blueprint_artifact_validate`, and `blueprint_state_sync`.
    - `tests/help-progress-health.test.ts` proves read-path status, repair-mode behavior, and runtime-doc alignment.
---

<objective>
Implement the Phase 2 read-path and repair command layer.

Purpose: Make the Wave 0 router/status/health surfaces useful against real `.blueprint/` state, including partial initialization and repair flows.
Output: shared state/artifact inspector tools, `help` / `progress` / `health` command contracts, and runtime-facing docs that match the shipped command surface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-router-health-and-mapping/02-CONTEXT.md
@.planning/phases/02-router-health-and-mapping/02-RESEARCH.md
@AGENTS.md
@docs/ARTIFACT-SCHEMA.md
@docs/MCP-TOOLS.md
@docs/commands/root-router.md
@docs/commands/help.md
@docs/commands/progress.md
@docs/commands/health.md
@src/mcp/server.ts
@src/mcp/tools/project.ts
@src/mcp/tools/state.ts
@src/mcp/tools/artifacts.ts
@GEMINI.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared state and artifact inspectors for read-path and repair flows</name>
  <files>src/mcp/server.ts, src/mcp/tools/project.ts, src/mcp/tools/state.ts, src/mcp/tools/artifacts.ts</files>
  <read_first>AGENTS.md, docs/ARTIFACT-SCHEMA.md, docs/MCP-TOOLS.md, docs/commands/help.md, docs/commands/progress.md, docs/commands/health.md, src/mcp/server.ts, src/mcp/tools/project.ts, src/mcp/tools/state.ts, src/mcp/tools/artifacts.ts, .planning/phases/02-router-health-and-mapping/02-RESEARCH.md</read_first>
  <action>Extend the MCP read-path surface with `blueprint_state_load`, `blueprint_state_sync`, `blueprint_artifact_list`, and `blueprint_artifact_validate`. `blueprint_state_load` must return `{state, blockers, derivedStatus}` where `derivedStatus` includes the effective project status, current phase, next action, and whether blockers are present. `blueprint_state_sync` must rebuild `STATE.md` fields from `.blueprint/STATE.md`, `.blueprint/ROADMAP.md`, and artifact presence, then return `{syncedFields, warnings}`. `blueprint_artifact_list` must return `{artifacts, reports, missing}` for core project artifacts, phase artifacts, and codebase artifacts when present. `blueprint_artifact_validate` must return `{valid, issues, suggestedRepairs}` and flag malformed config, missing core files, and incomplete artifact bundles. Refine `blueprintProjectStatus` where needed so it distinguishes no `.blueprint/`, partial `.blueprint/`, and initialized repos consistently, then register all new tool names in `src/mcp/server.ts`.</action>
  <verify>rg -n 'blueprint_state_load|blueprint_state_sync|blueprint_artifact_list|blueprint_artifact_validate|derivedStatus|syncedFields|suggestedRepairs' src/mcp/server.ts src/mcp/tools/project.ts src/mcp/tools/state.ts src/mcp/tools/artifacts.ts</verify>
  <acceptance_criteria>
    - `src/mcp/tools/state.ts` contains `blueprint_state_load`
    - `src/mcp/tools/state.ts` contains `blueprint_state_sync`
    - `src/mcp/tools/state.ts` contains `derivedStatus`
    - `src/mcp/tools/artifacts.ts` contains `blueprint_artifact_list`
    - `src/mcp/tools/artifacts.ts` contains `blueprint_artifact_validate`
    - `src/mcp/tools/artifacts.ts` contains `valid`, `issues`, and `suggestedRepairs`
    - `src/mcp/server.ts` contains all four new tool names
  </acceptance_criteria>
  <done>The read-path commands have shared MCP inspectors instead of prompt-only heuristics.</done>
</task>

<task type="auto">
  <name>Task 2: Author `help`, `progress`, and `health` command contracts and refresh runtime-facing docs</name>
  <files>GEMINI.md, README.md, commands/blu/help.toml, commands/blu/progress.toml, commands/blu/health.toml</files>
  <read_first>docs/commands/root-router.md, docs/commands/help.md, docs/commands/progress.md, docs/commands/health.md, docs/MCP-TOOLS.md, GEMINI.md, README.md, commands/blu.toml, commands/blu/new-project.toml, commands/blu/help.toml, commands/blu/progress.toml, commands/blu/health.toml, src/mcp/server.ts</read_first>
  <action>Create `commands/blu/help.toml` so it reads `blueprint_command_catalog` and `blueprint_project_status` and returns safe routing guidance with no writes. Create `commands/blu/progress.toml` so it reads `blueprint_project_status`, `blueprint_config_get`, `blueprint_state_load`, `blueprint_artifact_list`, and `blueprint_command_catalog`, then summarizes active profile, branching mode, warnings, blockers, and the next safe action. Create `commands/blu/health.toml` so it accepts `[--repair]`, reads `blueprint_project_status`, `blueprint_config_get`, `blueprint_config_set`, `blueprint_state_load`, `blueprint_artifact_list`, `blueprint_artifact_validate`, and `blueprint_state_sync`, and requires an explicit confirmation-style path before any repair write. Update `GEMINI.md` so the runtime command list includes `/blu:new-project`, `/blu:settings`, `/blu:set-profile`, `/blu:help`, `/blu:progress`, and `/blu:health`, and keep the `.blueprint/` versus `.planning/` boundary explicit. Update `README.md` by changing the opening description to say Blueprint is in active implementation, replacing the `## Planned Runtime Layout` heading with `## Current Runtime Layout`, and listing `gemini-extension.json`, `GEMINI.md`, `commands/blu.toml`, `commands/blu/new-project.toml`, `commands/blu/help.toml`, `commands/blu/progress.toml`, and `commands/blu/health.toml` as current runtime files.</action>
  <verify>rg -n 'blueprint_command_catalog|blueprint_project_status|blueprint_config_get|blueprint_state_load|blueprint_artifact_list|blueprint_artifact_validate|blueprint_state_sync|--repair|/blu:settings|/blu:progress|/blu:health|\\.planning' GEMINI.md README.md commands/blu/help.toml commands/blu/progress.toml commands/blu/health.toml</verify>
  <acceptance_criteria>
    - `commands/blu/help.toml` exists
    - `commands/blu/help.toml` contains `blueprint_command_catalog` and `blueprint_project_status`
    - `commands/blu/progress.toml` exists
    - `commands/blu/progress.toml` contains `blueprint_config_get`, `blueprint_state_load`, and `blueprint_artifact_list`
    - `commands/blu/health.toml` exists
    - `commands/blu/health.toml` contains `--repair`, `blueprint_artifact_validate`, and `blueprint_state_sync`
    - `GEMINI.md` contains `/blu:settings`, `/blu:set-profile`, `/blu:help`, `/blu:progress`, and `/blu:health`
    - `README.md` contains `active implementation`
    - `README.md` contains `## Current Runtime Layout`
  </acceptance_criteria>
  <done>The read-path command layer is documented and exposed through direct Gemini command contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture-backed tests for status inspection, repair behavior, and runtime-doc alignment</name>
  <files>tests/help-progress-health.test.ts, tests/fixtures/help-progress-health/</files>
  <read_first>AGENTS.md, docs/TEST-STRATEGY.md, docs/commands/help.md, docs/commands/progress.md, docs/commands/health.md, src/mcp/server.ts, src/mcp/tools/project.ts, src/mcp/tools/state.ts, src/mcp/tools/artifacts.ts, GEMINI.md, README.md, tests/help-progress-health.test.ts, tests/new-project.test.ts, tests/phase-01-validation.test.ts</read_first>
  <action>Create `tests/help-progress-health.test.ts` plus any needed fixtures under `tests/fixtures/help-progress-health/` for initialized, partial, and legacy-config repos. Reuse the temp-repo fixture pattern. Assert that `blueprintProjectStatus`, `blueprint_state_load`, `blueprint_artifact_list`, and `blueprint_artifact_validate` distinguish uninitialized, partial, and initialized repos correctly; `blueprint_state_sync` can repair state from surviving artifacts; the three new command TOMLs reference the exact tool names registered in `src/mcp/server.ts`; and `GEMINI.md` plus `README.md` mention the shipped command coverage instead of a docs-only runtime description.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `tests/help-progress-health.test.ts` exists
    - `tests/help-progress-health.test.ts` asserts uninitialized, partial, and initialized status cases
    - `tests/help-progress-health.test.ts` asserts `blueprint_state_sync`
    - `tests/help-progress-health.test.ts` asserts `blueprint_artifact_validate`
    - `tests/help-progress-health.test.ts` asserts `commands/blu/help.toml`, `commands/blu/progress.toml`, and `commands/blu/health.toml` reference registered tool names
    - `tests/help-progress-health.test.ts` asserts `GEMINI.md` contains `/blu:help` and `/blu:health`
    - `tests/help-progress-health.test.ts` asserts `README.md` contains `active implementation` and `## Current Runtime Layout`
  </acceptance_criteria>
  <done>The read-path and repair flows are covered by deterministic status and contract tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] `rg -n 'blueprint_state_load|blueprint_state_sync|blueprint_artifact_list|blueprint_artifact_validate|/blu:health|/blu:progress|/blu:help' src/mcp/tools/*.ts commands/blu/*.toml GEMINI.md README.md tests/help-progress-health.test.ts` finds the expected strings
</verification>

<success_criteria>

- All tasks completed
- `help`, `progress`, and `health` return guidance from real `.blueprint/` state
- Partial initialization and repair flows are explicit rather than heuristic
- Runtime-facing docs match the shipped command surface

</success_criteria>

<output>
After completion, create `.planning/phases/02-router-health-and-mapping/02-02-SUMMARY.md`
</output>
