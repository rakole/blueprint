---
phase: 01-foundation-bootstrap-and-state
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - gemini-extension.json
  - GEMINI.md
  - commands/blu.toml
  - package.json
  - tsconfig.json
autonomous: true
requirements:
  - FND-01
  - FND-02
user_setup: []
must_haves:
  truths:
    - The repository has a root Gemini extension manifest that points to `GEMINI.md`.
    - The extension exposes a root `/blu` entrypoint without relying on slash-command chaining.
    - The repo can build TypeScript runtime sources into `dist/` with standard npm scripts.
  artifacts:
    - gemini-extension.json
    - GEMINI.md
    - commands/blu.toml
    - package.json
    - tsconfig.json
  key_links:
    - gemini-extension.json references `GEMINI.md` as the context file.
    - gemini-extension.json points its MCP server entry at the built runtime under `dist/`.
    - package.json scripts compile `src/` into `dist/`.
---

<objective>
Create the installable extension shell and root router entrypoint for Blueprint.

Purpose: Establish the smallest Gemini-native runtime frame that later MCP and command work can plug into without changing the product contract.
Output: Build/test scaffolding, `gemini-extension.json`, `GEMINI.md`, and `commands/blu.toml`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md
@.planning/phases/01-foundation-bootstrap-and-state/01-RESEARCH.md
@AGENTS.md
@docs/DECISIONS.md
@docs/ARCHITECTURE.md
@docs/GEMINI-CONSTRAINTS.md
@docs/IMPLEMENTATION-ORDER.md
@docs/commands/root-router.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a buildable TypeScript extension scaffold</name>
  <files>package.json, tsconfig.json</files>
  <read_first>AGENTS.md, docs/ARCHITECTURE.md, docs/GEMINI-CONSTRAINTS.md, .planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md, .planning/phases/01-foundation-bootstrap-and-state/01-RESEARCH.md</read_first>
  <action>Create `package.json` and `tsconfig.json` for a Node 20+ TypeScript extension project. Define `build`, `test`, and `typecheck` npm scripts. Compile `src/**/*.ts` into `dist/`, keep install-time setup out of scripts, and make `dist/mcp/server.js` the expected built MCP entrypoint.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `package.json` contains `"name": "blueprint"`
    - `package.json` contains `"build"` and `"test"` scripts
    - `tsconfig.json` contains `"outDir": "dist"`
    - `tsconfig.json` includes the `src/` tree for compilation
  </acceptance_criteria>
  <done>`package.json` and `tsconfig.json` exist and support a buildable extension scaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Create the root extension manifest and context file</name>
  <files>gemini-extension.json, GEMINI.md</files>
  <read_first>docs/DECISIONS.md, docs/ARCHITECTURE.md, docs/GEMINI-CONSTRAINTS.md, docs/commands/new-project.md, .planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md, .planning/phases/01-foundation-bootstrap-and-state/01-RESEARCH.md</read_first>
  <action>Create `gemini-extension.json` with `name` set to `blueprint`, an initial version string, `contextFileName` set to `GEMINI.md`, and an `mcpServers.blueprint` entry that runs `node dist/mcp/server.js`. Create `GEMINI.md` that explains the `/blu` and `/blu:<command>` namespace, the `.blueprint/` and `~/.gemini/blueprint/` state boundary, and the rule that MCP owns persistent state mutations.</action>
  <verify>rg -n '"contextFileName"|mcpServers|/blu:new-project|\\.blueprint/' gemini-extension.json GEMINI.md</verify>
  <acceptance_criteria>
    - `gemini-extension.json` contains `"name": "blueprint"`
    - `gemini-extension.json` contains `"contextFileName": "GEMINI.md"`
    - `gemini-extension.json` contains `"mcpServers"` and `dist/mcp/server.js`
    - `GEMINI.md` contains `/blu:new-project`
    - `GEMINI.md` contains `.blueprint/` and `~/.gemini/blueprint/`
  </acceptance_criteria>
  <done>The extension root metadata matches the locked Blueprint contract.</done>
</task>

<task type="auto">
  <name>Task 3: Author the root `/blu` router command contract</name>
  <files>commands/blu.toml</files>
  <read_first>docs/commands/root-router.md, docs/MCP-TOOLS.md, GEMINI.md, .planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md</read_first>
  <action>Create `commands/blu.toml` as the root router command. Its prompt must inspect project status through `blueprint_project_status`, use `blueprint_command_catalog` for routing metadata, prefer safe inline routing when intent is clear, recommend direct commands when intent is ambiguous, and explicitly avoid slash-command chaining or undocumented aliases.</action>
  <verify>rg -n 'blueprint_project_status|blueprint_command_catalog|new-project|slash-command chaining|/blu' commands/blu.toml</verify>
  <acceptance_criteria>
    - `commands/blu.toml` exists
    - `commands/blu.toml` contains `blueprint_project_status`
    - `commands/blu.toml` contains `blueprint_command_catalog`
    - `commands/blu.toml` contains `/blu` and `new-project`
    - `commands/blu.toml` contains guidance to avoid slash-command chaining
  </acceptance_criteria>
  <done>The root router contract exists and points at the documented read-only routing flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] `rg -n '"contextFileName"|blueprint_project_status|blueprint_command_catalog' gemini-extension.json GEMINI.md commands/blu.toml` finds the expected strings
</verification>

<success_criteria>

- All tasks completed
- The repo has a root Gemini extension shell that can build into `dist/`
- The extension manifest points Gemini at `GEMINI.md`
- The root `/blu` command contract exists and follows the documented routing behavior

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bootstrap-and-state/01-01-SUMMARY.md`
</output>
