---
phase: 01-foundation-bootstrap-and-state
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/mcp/server.ts
  - src/mcp/tools/project.ts
  - src/mcp/tools/config.ts
  - src/mcp/tools/state.ts
  - src/mcp/tools/artifacts.ts
autonomous: true
requirements:
  - FND-01
  - FND-03
user_setup: []
must_haves:
  truths:
    - The built MCP server registers the project, config, state, and artifact tools needed by `/blu:new-project`.
    - `blueprint_project_init` can create a deterministic `.blueprint/` scaffold from locked defaults.
    - Config and state writes use normalized, path-safe helper logic instead of ad hoc filesystem mutations.
  artifacts:
    - src/mcp/server.ts
    - src/mcp/tools/project.ts
    - src/mcp/tools/config.ts
    - src/mcp/tools/state.ts
    - src/mcp/tools/artifacts.ts
  key_links:
    - src/mcp/server.ts registers the exact tool names implemented in `src/mcp/tools/*.ts`.
    - `blueprint_project_init` uses config, state, and artifact helpers instead of duplicating write logic.
    - Config normalization matches the v2 schema defined in `docs/ARTIFACT-SCHEMA.md`.
---

<objective>
Bootstrap the Blueprint MCP server and the first deterministic tool surfaces.

Purpose: Make the Phase 1 command path real by moving persistence into explicit tool handlers that follow the repo's locked contracts.
Output: `src/mcp/server.ts` plus project, config, state, and artifact tool modules that support `/blu:new-project`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md
@.planning/phases/01-foundation-bootstrap-and-state/01-RESEARCH.md
@AGENTS.md
@docs/ARCHITECTURE.md
@docs/ARTIFACT-SCHEMA.md
@docs/MCP-TOOLS.md
@docs/commands/new-project.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the MCP server entrypoint and tool registry</name>
  <files>src/mcp/server.ts</files>
  <read_first>docs/ARCHITECTURE.md, docs/MCP-TOOLS.md, gemini-extension.json, package.json, tsconfig.json, .planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md</read_first>
  <action>Create `src/mcp/server.ts` as the stdio MCP entrypoint used by the extension manifest. Register these exact tool names with stable JSON contracts: `blueprint_project_init`, `blueprint_project_status`, `blueprint_config_get`, `blueprint_config_set`, `blueprint_state_update`, and `blueprint_artifact_scaffold`. Load the handlers from `src/mcp/tools/*.ts`, reject unknown tool names, and keep server bootstrap small enough that future tools can extend the registry without rewriting the entrypoint.</action>
  <verify>rg -n 'blueprint_project_init|blueprint_project_status|blueprint_config_get|blueprint_config_set|blueprint_state_update|blueprint_artifact_scaffold' src/mcp/server.ts</verify>
  <acceptance_criteria>
    - `src/mcp/server.ts` exists
    - `src/mcp/server.ts` contains all six required Phase 1 tool names
    - `src/mcp/server.ts` imports handlers from `src/mcp/tools/`
    - `src/mcp/server.ts` is the file targeted by the manifest build output (`dist/mcp/server.js`)
  </acceptance_criteria>
  <done>The MCP runtime entrypoint is ready to host the first Blueprint tool surface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement project and config primitives for deterministic bootstrap</name>
  <files>src/mcp/tools/project.ts, src/mcp/tools/config.ts</files>
  <read_first>docs/ARTIFACT-SCHEMA.md, docs/MCP-TOOLS.md, docs/commands/new-project.md, .planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md, .planning/phases/01-foundation-bootstrap-and-state/01-RESEARCH.md</read_first>
  <action>Implement `blueprint_project_init` and `blueprint_project_status` in `src/mcp/tools/project.ts`. `blueprint_project_init` must create `.blueprint/PROJECT.md`, `.blueprint/REQUIREMENTS.md`, `.blueprint/ROADMAP.md`, `.blueprint/STATE.md`, `.blueprint/config.json`, and `.blueprint/phases/` for a fresh repo. Implement `blueprint_config_get` and `blueprint_config_set` in `src/mcp/tools/config.ts` so repo config is written in normalized v2 form, merges hardcoded defaults with optional user defaults, and excludes repo-level `hooks.*`, `workflow.use_workspaces`, and `workflow.use_workstreams` keys.</action>
  <verify>rg -n '\\.blueprint/(PROJECT|REQUIREMENTS|ROADMAP|STATE)\\.md|config\\.json|version|workflow|parallelization|hooks' src/mcp/tools/project.ts src/mcp/tools/config.ts</verify>
  <acceptance_criteria>
    - `src/mcp/tools/project.ts` contains `blueprint_project_init`
    - `src/mcp/tools/project.ts` contains `blueprint_project_status`
    - `src/mcp/tools/project.ts` references `.blueprint/PROJECT.md`, `.blueprint/REQUIREMENTS.md`, `.blueprint/ROADMAP.md`, `.blueprint/STATE.md`, `.blueprint/config.json`, and `.blueprint/phases/`
    - `src/mcp/tools/config.ts` writes a config object with `"version": 2`
    - `src/mcp/tools/config.ts` contains `workflow` and `parallelization` sections
    - `src/mcp/tools/config.ts` omits or rejects repo-level `hooks` keys
  </acceptance_criteria>
  <done>Project bootstrap and config normalization are implemented behind MCP tools.</done>
</task>

<task type="auto">
  <name>Task 3: Implement state and artifact helpers required by `new-project`</name>
  <files>src/mcp/tools/state.ts, src/mcp/tools/artifacts.ts</files>
  <read_first>docs/ARTIFACT-SCHEMA.md, docs/MCP-TOOLS.md, docs/commands/new-project.md, src/mcp/tools/project.ts, src/mcp/tools/config.ts</read_first>
  <action>Implement `blueprint_state_update` in `src/mcp/tools/state.ts` and `blueprint_artifact_scaffold` in `src/mcp/tools/artifacts.ts`. The state tool must patch `.blueprint/STATE.md` deterministically and return `{updatedFields, statePath}`. The artifact tool must create or reuse requested artifacts for the bootstrap flow and return `{createdFiles, reusedFiles, warnings}`. Both modules must keep writes repo-relative, reject path traversal, and share helper logic instead of duplicating filesystem rules.</action>
  <verify>rg -n 'blueprint_state_update|updatedFields|statePath|blueprint_artifact_scaffold|createdFiles|reusedFiles|warnings|path traversal' src/mcp/tools/state.ts src/mcp/tools/artifacts.ts</verify>
  <acceptance_criteria>
    - `src/mcp/tools/state.ts` contains `blueprint_state_update`
    - `src/mcp/tools/state.ts` returns `updatedFields` and `statePath`
    - `src/mcp/tools/artifacts.ts` contains `blueprint_artifact_scaffold`
    - `src/mcp/tools/artifacts.ts` returns `createdFiles`, `reusedFiles`, and `warnings`
    - Both modules implement repo-relative write guards
  </acceptance_criteria>
  <done>The Phase 1 state and artifact mutation helpers exist and are safe to call from `new-project`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] `rg -n 'blueprint_project_init|blueprint_config_get|blueprint_state_update|blueprint_artifact_scaffold' src/mcp/server.ts src/mcp/tools/*.ts` finds the expected tool names
</verification>

<success_criteria>

- All tasks completed
- The MCP server exposes the exact Phase 1 tool names needed by `new-project`
- `blueprint_project_init` can scaffold deterministic `.blueprint/` state
- Config, state, and artifact writes are centralized in tool modules rather than embedded in prompts

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bootstrap-and-state/01-02-SUMMARY.md`
</output>
