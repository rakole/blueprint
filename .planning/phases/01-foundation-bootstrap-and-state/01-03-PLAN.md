---
phase: 01-foundation-bootstrap-and-state
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - commands/blu/new-project.toml
  - tests/new-project.test.ts
  - tests/fixtures/new-project/
autonomous: true
requirements:
  - FND-02
  - FND-03
user_setup: []
must_haves:
  truths:
    - The extension contains a `/blu:new-project` command contract that uses the documented MCP tool flow.
    - Happy-path tests prove a fresh repo can be initialized into deterministic `.blueprint/` artifacts and normalized config.
    - The generated project status after initialization points users toward the next safe command rather than leaving the repo ambiguous.
  artifacts:
    - commands/blu/new-project.toml
    - tests/new-project.test.ts
    - tests/fixtures/new-project/
  key_links:
    - `commands/blu/new-project.toml` references the same tool names registered in `src/mcp/server.ts`.
    - Tests assert the exact `.blueprint/` artifact paths created by `blueprint_project_init`.
    - Tests verify the generated config matches the normalized v2 schema constraints.
---

<objective>
Wire the first mutating Blueprint command and prove the Phase 1 vertical slice with tests.

Purpose: Turn the extension shell and MCP primitives into an end-to-end `new-project` flow that can seed a repo deterministically.
Output: `commands/blu/new-project.toml` plus fixture-based tests for project initialization and status.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md
@.planning/phases/01-foundation-bootstrap-and-state/01-RESEARCH.md
@AGENTS.md
@docs/MCP-TOOLS.md
@docs/commands/new-project.md
@commands/blu.toml
@src/mcp/server.ts
@src/mcp/tools/project.ts
@src/mcp/tools/config.ts
@src/mcp/tools/state.ts
@src/mcp/tools/artifacts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the `/blu:new-project` command contract</name>
  <files>commands/blu/new-project.toml</files>
  <read_first>docs/commands/new-project.md, docs/MCP-TOOLS.md, GEMINI.md, commands/blu.toml, src/mcp/server.ts, .planning/phases/01-foundation-bootstrap-and-state/01-CONTEXT.md</read_first>
  <action>Create `commands/blu/new-project.toml` as the first mutating direct command. The prompt must support the documented `[--auto]` flag, call the `blueprint_project_init`, `blueprint_project_status`, `blueprint_config_get`, `blueprint_config_set`, `blueprint_state_update`, and `blueprint_artifact_scaffold` tools, summarize the created `.blueprint/` paths, and end by telling the user the next safe action after initialization.</action>
  <verify>rg -n 'blueprint_project_init|blueprint_project_status|blueprint_config_get|blueprint_config_set|blueprint_state_update|blueprint_artifact_scaffold|--auto|\\.blueprint/' commands/blu/new-project.toml</verify>
  <acceptance_criteria>
    - `commands/blu/new-project.toml` exists
    - `commands/blu/new-project.toml` contains `--auto`
    - `commands/blu/new-project.toml` contains all six documented Phase 1 tool names
    - `commands/blu/new-project.toml` contains `.blueprint/config.json`
    - `commands/blu/new-project.toml` includes a next-step summary for the user
  </acceptance_criteria>
  <done>The first direct Blueprint command exists and is wired to the documented MCP flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture-based happy-path tests for project initialization</name>
  <files>tests/new-project.test.ts, tests/fixtures/new-project/</files>
  <read_first>AGENTS.md, docs/ARTIFACT-SCHEMA.md, docs/commands/new-project.md, src/mcp/tools/project.ts, src/mcp/tools/config.ts, src/mcp/tools/state.ts, src/mcp/tools/artifacts.ts, package.json</read_first>
  <action>Create a fresh-repo fixture under `tests/fixtures/new-project/` and a test file at `tests/new-project.test.ts`. Invoke the project initialization flow through the MCP tool handlers or their nearest public runtime surface. Assert that `.blueprint/PROJECT.md`, `.blueprint/REQUIREMENTS.md`, `.blueprint/ROADMAP.md`, `.blueprint/STATE.md`, `.blueprint/config.json`, and `.blueprint/phases/` are created. Assert that the generated config contains `\"version\": 2`, `workflow`, `parallelization`, `safety`, and `maintenance` sections, and that it does not persist repo-level `hooks` keys.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `tests/new-project.test.ts` exists
    - `tests/new-project.test.ts` asserts creation of `.blueprint/PROJECT.md`
    - `tests/new-project.test.ts` asserts creation of `.blueprint/REQUIREMENTS.md`
    - `tests/new-project.test.ts` asserts creation of `.blueprint/ROADMAP.md`
    - `tests/new-project.test.ts` asserts creation of `.blueprint/STATE.md`
    - `tests/new-project.test.ts` asserts creation of `.blueprint/config.json`
    - `tests/new-project.test.ts` asserts creation of `.blueprint/phases/`
    - `tests/new-project.test.ts` asserts `"version": 2`
    - `tests/new-project.test.ts` asserts repo-level `hooks` keys are absent from persisted config
  </acceptance_criteria>
  <done>The bootstrap flow is covered by deterministic happy-path tests.</done>
</task>

<task type="auto">
  <name>Task 3: Verify post-init status and command-to-tool integration</name>
  <files>tests/new-project.test.ts</files>
  <read_first>docs/MCP-TOOLS.md, docs/commands/new-project.md, commands/blu/new-project.toml, src/mcp/server.ts, src/mcp/tools/project.ts</read_first>
  <action>Extend the Phase 1 test coverage so it verifies that `blueprint_project_status` reports the repo as initialized after bootstrap and returns a non-empty next action. Also assert that the command contract references the exact tool names registered in `src/mcp/server.ts`, so command text and runtime tool registration cannot drift silently.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `tests/new-project.test.ts` asserts `blueprint_project_status` returns `initialized: true`
    - `tests/new-project.test.ts` asserts `blueprint_project_status` returns a non-empty `nextAction`
    - `tests/new-project.test.ts` checks that the command contract and server tool registry use matching Phase 1 tool names
  </acceptance_criteria>
  <done>The end-to-end Phase 1 bootstrap loop is covered by command and status integration checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] `rg -n 'blueprint_project_init|blueprint_project_status|--auto' commands/blu/new-project.toml tests/new-project.test.ts` finds the expected strings
</verification>

<success_criteria>

- All tasks completed
- `/blu:new-project` is defined as a direct Blueprint command contract
- Tests prove deterministic `.blueprint/` bootstrap and normalized config creation
- Post-init status returns a clear next step for the user

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bootstrap-and-state/01-03-SUMMARY.md`
</output>
