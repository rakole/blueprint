---
phase: 02.2-urgent-drift-repair-follow-up
plan: "04"
type: execute
wave: 3
depends_on:
  - 02.2-01
  - 02.2-02
  - 02.2-03
files_modified:
  - tests/command-catalog.test.ts
  - tests/command-contract-docs.test.ts
  - tests/drift-repair-docs.test.ts
autonomous: true
requirements:
  - DRIFT-07
user_setup: []
must_haves:
  truths:
    - Regression tests fail if control-plane docs drift from the active Phase 2.2 checkpoint.
    - Regression tests fail if future command ownership or command-contract metadata drifts across canonical doc surfaces.
    - Regression tests fail if blocked commands become effectively routable without the required runtime substrate.
  artifacts:
    - tests/command-catalog.test.ts
    - tests/command-contract-docs.test.ts
    - tests/drift-repair-docs.test.ts
  key_links:
    - `tests/command-catalog.test.ts` locks the derived `implemented` versus blocked command behavior from `src/mcp/tools/project.ts`.
    - `tests/command-contract-docs.test.ts` compares canonical command metadata across the catalog, skills inventory, migration matrix, and command specs.
    - `tests/drift-repair-docs.test.ts` keeps control-plane docs and planning-state docs aligned with the active drift ledger.
---

<objective>
Add regression coverage that locks the repaired control plane and future command contracts in place.

Purpose: Make DRIFT-07 enforceable by code, not memory, while preserving the runtime catalog semantics already present in `src/mcp/tools/project.ts`.
Output: Hardened drift-repair regression tests for runtime command exposure, cross-doc command contracts, and control-plane truth sync.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-urgent-drift-repair-follow-up/02.2-VALIDATION.md
@docs/DRIFT.MD
@docs/COMMAND-CATALOG.md
@docs/SKILLS-AND-AGENTS.md
@docs/GSD-RUNTIME-MIGRATION.md
@src/mcp/tools/project.ts
@tests/command-catalog.test.ts
@tests/command-contract-docs.test.ts
@tests/drift-repair-docs.test.ts

<interfaces>
From src/mcp/tools/project.ts:
```typescript
type CommandStatus = "planned" | "implemented" | "blocked" | "repairing";

type CommandCatalogEntry = {
  declaredStatus: CommandStatus;
  status: CommandStatus;
  implemented: boolean;
  blockedBy: string[];
  requiredToolsSatisfied: boolean;
  manifestPath: string | null;
  skillPath: string | null;
  specPath: string | null;
  optionalAgents: string[];
  availableOptionalAgents: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden runtime and cross-doc regression assertions around the repaired baseline</name>
  <files>tests/command-catalog.test.ts, tests/command-contract-docs.test.ts</files>
  <read_first>src/mcp/tools/project.ts, docs/COMMAND-CATALOG.md, docs/SKILLS-AND-AGENTS.md, docs/GSD-RUNTIME-MIGRATION.md, docs/commands/next.md, docs/commands/do.md, docs/commands/pause-work.md, docs/commands/resume-work.md, docs/commands/plan-milestone-gaps.md, tests/command-catalog.test.ts, tests/command-contract-docs.test.ts, .planning/phases/02.2-urgent-drift-repair-follow-up/02.2-VALIDATION.md</read_first>
  <action>Extend the existing tests to lock the repaired baseline rather than rewriting the runtime model. In `tests/command-catalog.test.ts`, keep `src/mcp/tools/project.ts` semantics unchanged and add assertions only around the current implemented-versus-blocked surface, required-tool satisfaction, and blocked-reason expectations for known future commands. In `tests/command-contract-docs.test.ts`, strengthen cross-doc checks for the known ownership mismatch set and any high-risk spec structure needed to preserve D-07 and D-08. Do not modify `src/mcp/tools/project.ts`, do not change command statuses in docs to satisfy tests, and do not assume a globally installed `tsx` binary.</action>
  <verify>
    <automated>node_modules/.bin/tsx --test tests/command-catalog.test.ts tests/command-contract-docs.test.ts tests/drift-repair-docs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - runtime-catalog assertions still treat only shipped Wave 0 commands as implemented
    - cross-doc assertions cover the repaired ownership mismatch set
    - test commands use `node_modules/.bin/tsx` assumptions only in plan verification, not hardcoded product behavior
  </acceptance_criteria>
  <done>The runtime and cross-doc drift tests enforce the repaired baseline without altering runtime semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Expand control-plane drift tests and ratify the full suite</name>
  <files>tests/drift-repair-docs.test.ts</files>
  <read_first>docs/DRIFT.MD, README.md, GEMINI.md, docs/HANDOFF.md, MEMORY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, tests/drift-repair-docs.test.ts, package.json, .planning/phases/02.2-urgent-drift-repair-follow-up/02.2-VALIDATION.md</read_first>
  <action>Update `tests/drift-repair-docs.test.ts` so it locks the Phase 2.2 control-plane truth sync, requirement traceability, and freeze semantics against future regression. Make assertions specific to the repaired 2026-04-11 Phase 2.1 closure and active Phase 2.2 checkpoint, and keep the tests focused on control-plane and contract drift rather than generic content snapshots. Finish by running the fast drift suite and the full `npm test` suite as required by the validation contract.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/drift-repair-docs.test.ts` asserts active-checkpoint truth sync across the canonical control-plane files
    - `npm test` passes after the targeted drift suite passes
    - the regression layer would fail if Phase 3+ exposure or ownership metadata drift reappears
  </acceptance_criteria>
  <done>The repaired control plane and command contracts are protected by both the fast drift suite and the full test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs and catalog metadata -> runtime guidance | Drift here can surface blocked commands or hide missing substrate |
| regression suite -> release gate | Weak or stale tests can let contract flattening or control-plane drift ship unnoticed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-04-01 | T | `tests/command-catalog.test.ts`, `src/mcp/tools/project.ts` | mitigate | Lock the currently implemented Wave 0 set and blocked-command reasons without changing `project.ts` semantics |
| T-02.2-04-02 | T | `tests/command-contract-docs.test.ts`, `tests/drift-repair-docs.test.ts` | mitigate | Assert ownership metadata, control-plane checkpoint language, and traceability across all canonical surfaces so drift fails fast |
| T-02.2-04-03 | D | local verification workflow | mitigate | Use the repo-local `node_modules/.bin/tsx` quick suite and `npm test` full suite from the validation contract instead of relying on missing global tooling |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node_modules/.bin/tsx --test tests/command-catalog.test.ts tests/command-contract-docs.test.ts tests/drift-repair-docs.test.ts` passes
- [ ] `npm test` passes
- [ ] No file outside `tests/` was modified for this plan
</verification>

<success_criteria>

- All tasks completed
- The drift-repair regression suite fails on control-plane drift, command-contract drift, or blocked-command exposure drift
- `src/mcp/tools/project.ts` semantics remain unchanged during Phase 2.2
- The phase validation contract is satisfied with local test commands only

</success_criteria>

<output>
After completion, create `.planning/phases/02.2-urgent-drift-repair-follow-up/02.2-04-SUMMARY.md`
</output>
