---
status: in-progress
date: 2026-05-07
task: review-fix-parity-implementation
---

# Quick Task: Review / Review-Fix Contract Parity

Implement the user-approved recommendations from `docs/reviews/code-review-review-fix-parity-2026-05-07.md`.

## Locked Decisions

- Add an explicit read-only `/blu-code-review-fix` reclassification contract to `blueprint-reviewer`, and make inline fallback guidance equivalent.
- Expose all parsed findings, but default review-fix selection to `follow-up` findings only.
- Classify follow-ups; default only fixable items, allow explicit test-gap items, route validation/process/no-op notes away.
- Preserve visible finding ids; generate deterministic hash ids for legacy rows and mark them legacy-derived.
- Require `scopeSource` for code-review record writes when scoped files are present.
- Use balanced validator relaxation: keep safety gates hard, soften noisy checks, allow table pipes via renderer escaping, and harden markdown scalar rendering.
- Relax review-fix `PARTIAL` remaining-work policy and allow exact no-change completion only for follow-up/verification-only completion with passing verification.
- Keep security-first primary routing while surfacing review-fix as a secondary recommendation.
- Lock `/blu-code-review-fix` state update guidance to an explicit synced patch.
- Update runtime reference rows to source-owned metadata paths while retaining command docs as control-plane history.

## Tasks

1. Contracts and docs parity
   - Update command manifests, agent contract, review skill references, runtime reference, and metadata tests.
   - Verify JSON-model wording does not imply rendered heading keys are authorable.

2. Runtime parity
   - Preserve structured code-review finding data through load findings.
   - Classify review-fix targets and default selection policy.
   - Return structured invalid-record diagnostics.
   - Enforce explicit code-review scope provenance.
   - Apply selected validator relaxations and routing/state behavior.

3. Regression coverage and verification
   - Add focused tests for loader shape, target classification, target-id drift, diagnostics, scope provenance, validator policy, routing, and metadata.
   - Run focused review tests, typecheck, build, and full test suite.
