# Blueprint Script Instructions

Scripts in this repo support local validation and extension development.

## Rules

- Use Node.js ESM patterns to match the repository setup.
- Keep scripts safe to run from the repository root.
- Prefer validating real scaffold assumptions over placeholder output.
- Fail with clear error messages when required files or invariants are missing.
- Keep dependencies minimal unless the script materially benefits from them.

